# 修复总结 — Phase 480 画布插入信号共享租约修复

## 问题

Math 与照片插入的工具栏入口已有父页守卫，但画布接收信号的 `@Watch` 回调直接调用插入函数；
若信号在租约开始前入队，仍会依赖内部防御作为唯一防线。

## 修复

两个信号回调先拒绝共享照片导入租约，再执行原有插入路径。Math 的页面健康、历史与身份防御，
照片导入的能力门禁和上下文校验不变。

## 影响与验证

- 新增 ADR-0452、Harmony evidence 和本中文 Report。
- 新增专项 Replay，当前 `TOTAL=4 FAILED=0`。
- ArkTS 目标无错误。
- 相邻数学编辑 10/10、照片导入 12/12 通过。
- 全量 Desktop Replay 通过 421/421（31.856 秒）；clean 3.246 秒、ohosTest HAP 9.781 秒、
  default HAP 30.456 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
