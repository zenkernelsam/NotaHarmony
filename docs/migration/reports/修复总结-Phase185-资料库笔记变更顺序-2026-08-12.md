# Phase 185 修复总结：资料库笔记变更顺序

## 发现

资料库创建和删除使用独立 UI busy 标志，可以并发进入 `LibraryViewModel`，导致仓储
变更和列表刷新交错。查询 generation 不能替代写操作顺序。

## 修改

- `note/src/main/ets/ui/library/LibraryViewModel.ets`
- 新增 `ADR-0162-library-note-mutation-order.md`
- 新增 `d02-library-note-mutation-order.mjs`

创建和删除现在共享一个 promise 队列；事务及对应列表刷新按调用顺序完成，失败不会
阻塞队列后续操作。

## 验证

- Replay：`TOTAL=5 FAILED=0`
- HAP 构建：本轮未执行，仓库根目录无 `hvigorw`/`hvigorw.bat`
- 未启动设备、模拟器、虚拟机或 Hypium

## 未闭环

绕过资料库 ViewModel 的后台仓储写入仍需更高层统一写者策略。
