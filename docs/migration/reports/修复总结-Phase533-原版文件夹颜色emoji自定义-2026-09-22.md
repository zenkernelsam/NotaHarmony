# Phase 533：原版文件夹颜色 / emoji 自定义与编辑时间戳落地

日期：2026-09-22

## 选题

Phase 530（原版 Room schema 逐表比对）登记的 SCHEMA-D3：原版
`SyncedFolderMetadata`/`ClientFolderEdit` 持有 `color`/`emoji`/`updatedAt`，
且 `pdb` 显示模型逐字段解析、`w09` 笔记行携带 `folderColor`、emoji 选择器
为 flag 门控的真实功能；Harmony `folder` 此前仅 `id/name/created_at/parent_id/
sibling_order`，无颜色/emoji/编辑时间戳——判定为功能级差距（非仅同步字段）。

## 原版现场重放（decompiled_1.0.3）

- `e47.java:353-354`：`SyncedFolderMetadata.color INTEGER NOT NULL` +
  `emoji TEXT` + `updatedAt NOT NULL`；`ClientFolderEdit` 除键外全可空——
  "未写字段=保留"的增量编辑行。
- `xo1.java`：`d=color(Integer)`、`e=siblingOrder(Double)`、`g=updatedAt(long)`、
  `j=emoji(String)` 字段序。
- `id7.l`（重命名/自定义）一次性提交 title+color+emoji → `beb.e`；
  `id7.b`（创建）要求具体 `int color`；`rdb` 创建时 createdAt/updatedAt 同刻。
- `vdb`（beb.e 主体）：null 参数字段回退 `lq4Var` 现值（保留），`this.T`
  （updatedAt）无条件写入；`udb` 全 null 调用亦盖章——updatedAt 恒写。
- `xdb`（移动）：仅写 parentId+siblingOrder，余字段 null 保留。
- `pdb`：`f()` 颜色 edit→synced→抛错（恒具体 int）；`d()` emoji
  edit→synced，空串/ null → null；`ncj.c` 取最新编辑行。
- `w09.java`：笔记行 `folderColor`——用户可见。
- `ac4.java:133`/`msb.java`：`LIBRARY_FOLDER_EMOJI_PICKER` flag 与分类
  emoji 网格。

## Harmony 修改（ADR-0505）

- `DatabaseHelper.ets`：`folder` +`color INTEGER NOT NULL DEFAULT
  FOLDER_DEFAULT_COLOR(-7431250)`、`emoji TEXT`、`updated_at INTEGER NOT NULL
  DEFAULT 0`；版本 67→68，`MIGRATIONS[68]` 三列 ALTER +
  `updated_at` 回填 `created_at`。
- `FolderRepositoryImpl.ets`：`NoteFolder` +`color/emoji/updatedAt`；
  `createFolder` 带 color/emoji 参数且 `updatedAt=createdAt`；新
  `editFolder({ name?, color?, emoji? })`（beb.e 对齐：未写不动、单事务、
  `updated_at` 恒盖）；`renameFolder` 委托之；`moveFolder` 仅被移动文件夹盖章；
  `normalizeFolderEmoji`（`null/''`→NULL）；`queryFolders`/`folderBucket` 全列。
- `LibraryPage.ets`：`NameDialog` 增 8 色色块行 + `∅`/10 个预设 emoji 行，
  `onConfirm(name, color, emoji)`；新建默认色+无 emoji，重命名按当前值初始化；
  确认改走 `createFolder(…, color, emoji)` / `editFolder({name,color,emoji})`；
  `FolderNavigationRow` 名称前渲染 10dp 色点 + emoji（侧栏与紧凑抽屉共用）。
- `FolderRepository.test.ets`：`folder()` 工厂补 `color/emoji/updatedAt`；
  `DatabaseHelper.test.ets` 钉 `DB_VERSION = 68`。
- 既有 Replay 锚点跟进：三处 `DB_VERSION=67` 钉值与两处
  `onFolderDialogConfirm` 签名锚点同步到新签名/新版本（不变量语义不变）。

## 验证

- 专项 Replay `d02-original-folder-customization-parity.mjs`：56 项断言——
  原版锚点（e47/xo1/vdb/id7/xdb/pdb/w09/ac4/msb/rdb）+ Harmony
  schema/仓库/UI/fixture 锚点 + 可执行增量编辑模型（保留语义/盖章/空串清除）。
  结果 `TOTAL=56 FAILED=0`。
- 全量 Desktop Replay：`428/428`（仓根执行）。
- `hvigor clean` + `note@ohosTest` + `note@default`：全部 BUILD SUCCESSFUL，
  双 HAP 产出正常；0 个 ArkTS ERROR；触改文件仅库内既有 "may throw" 类警告。
- 未启动模拟器、虚拟机、真机或 Hypium；`T-042` 仍为 Goal 最后一项。

## 残留差异（如实登记）

1. emoji 选择器为 10 预设 + 清除的子集（原版为 flag 门控 9 大类全量网格）——
   后续可扩展分类面板。
2. 原版调色板确切色值未恢复；`FOLDER_COLOR_PALETTE`/`FOLDER_DEFAULT_COLOR`
   为文档化近似（ADR-0505）。
3. 笔记行 `folderColor` 渲染未做（原版 All Notes 行内显示文件夹色块）——
   Harmony 颜色已在文件夹行渲染，笔记行色块留作后续增强。
4. 无"清除颜色"语义（对齐原版 color 恒非空）。
