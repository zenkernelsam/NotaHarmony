# Phase 195 修复总结：系统 Backup 相对路径按段校验

## 发现

Backup 路径校验把任何包含 `..` 的合法文件名都当成目录穿越，导致整个备份失败。

## 修改

- `NoteBackupAbility.ets` 改为统一分隔符后逐 segment 校验
- 精确拒绝空段、`.`、`..`、绝对路径和盘符
- 新增 ADR、replay 和本报告

## 验证

- Replay：`TOTAL=5 FAILED=0`
- HAP/设备测试：本轮未执行
