# Phase 540：原版文件夹装饰页签与分类 Emoji 选择器落地

日期：2026-09-22

## 选题

Phase 533 登记遗留：原版文件夹对话框的 emoji 选择器是
`LIBRARY_FOLDER_EMOJI_PICKER` flag 门控的**分类网格**，且装饰区为
Color|Emoji 二选一页签（`hq4`）；Harmony 仅有 `∅`+10 预设行、
颜色行与 emoji 行始终并列——结构与交互均不符原版。

## 原版现场重放（decompiled_1.0.3）

- `hq4.java`：`Color(ui_folder__decoration_color)` /
  `Emoji(ui_folder__decoration_emoji)` 装饰枚举。
- `gaj.java`：flag 开 → `e(hq4,cb)` 页签行（`q31` 遍历 `hq4.M`）；
  初态 `emoji==null ? Color : Emoji`（`fsi.T`）；内容按 ordinal
  切换：Color→`d(6,…)` 六列色板，Emoji→`a(count,…)` 分类网格。
- `wm2.java` case 0：`== hq4.Color → emoji=null`——**装饰互斥**，
  切色页清 emoji。
- `du3.java`：九类序 Smileys/PeopleBody/AnimalsNature/FoodDrink/
  Activities/Objects/TravelPlaces/Symbols/Flags；`md.java`：
  `emojis_*` 矢量分类图标；`m6a` 管选中类与计数。
- `ru3.java`：emoji 本体为打包 Realm `EmojiInfo` 库——反编译
  不可恢复，Harmony 侧采用按原版分类序的策划子集。

## Harmony 落地（LibraryPage.ets `NameDialog`）

- 新增 `decorationTab`/`emojiCategory` 状态；`aboutToAppear` 按
  `initialEmoji` 落页（fsi.T 对齐）。
- 名称输入下渲染 `decorationTabButton` 分段页签行；Color 页保留
  8 色板，Emoji 页为九类分类条 + ∅ 片 + 七列网格（132 滚动高）。
- 切 Color 页清空 `selectedEmoji`（wm2 互斥）；`onConfirm` 契约
  不变。
- 文件夹行改为 emoji 优先、未设回落色点（与互斥一致的推断项）。
- 字符串：`decoration_color`/`decoration_emoji` +
  `emoji_category_0..8`（英/中）。

## 验证

- `d02-original-folder-emoji-picker.mjs`：39 断言全过。
- `d02-original-folder-customization-parity.mjs` 两处锚点合法演进
  （PRESETS→CATEGORIES）。
- 全套 replay：**435 PASS / 0 FAIL**；`note@default` +
  `note@ohosTest` BUILD SUCCESSFUL（仅存量警告）。

## 登记差异

- emoji 全集不可恢复 → 每类 16–23 个策划子集。
- 分类图标用代表 emoji 字形（原版为 `emojis_*` 矢量）。
- 原版网格搜索框（需逐 emoji 关键词）未移植——登记。
- 行渲染 emoji 优先为推断项。
