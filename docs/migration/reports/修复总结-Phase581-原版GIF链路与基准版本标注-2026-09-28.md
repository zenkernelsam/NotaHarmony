# Phase 581 — 原版 GIF 链路解码 + 基准版本标注修正（2026-09-28）

## 目标

两件小项合并：
1. 审计注册表硬性要求：`HANDOVER-COMMANDER.md` 把
   `reference/defpackage/` 标为"审计基准"但未注版本——补注其为
   **1.0.1** 混淆树（1.0.3 类名映射完全不同）。
2. 原版插入菜单 "Add GIF" 项的链路定性。

## 修正 1：基准版本标注

`reference/defpackage/` 实指桌面证据树 `decompiled_1.0.1/sources/
defpackage/`（Harmony 仓库内并不存在该目录）。已在两处（技术红线
第 58 行、结构速查第 90 行）补注版本号与混淆映射警告——这正是
注册表"基准版本标注问题"要求的修正，避免后续按 1.0.3 类名错查。

## 解码 2：Add GIF 链路

- `qc.java`（插入菜单）：GIF 项仅当 `function4 != null` 渲染——
  宿主透传的 picker 回调，经 `kkf.a`/`nti`/`rh8` 多层 lambda 转发，
  静态不可钉（与 Add Files 同盲区）。
- `oj3`：gif 在受支持图片 MIME 集内，文件 picker/拖放/剪贴板共用，
  无特殊路径 —— "Add GIF" 本质是图片 picker 的便捷入口。
- 动图渲染：原版 `ly/jy` 用 `AnimatedImageDrawable` 专用动画视图；
  Harmony 画布走静态 PixelMap 无等价物，`ImageAssetLoader` 已
  fail-closed 为 `ANIMATED_UNSUPPORTED`（资产可入库、渲染空态）。

## 验证

- 专项 `d02-original-gif-ingress-boundary.mjs`：7/7。
- 源码改动：仅 `HANDOVER-COMMANDER.md` 两处文档标注。
