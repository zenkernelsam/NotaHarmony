# 修复总结 — Phase 527 资料库笔记卡片打开入口激活边界修复

日期：2026-08-29

## 问题

资料库笔记卡片的单击回调直接调用编辑器路由，页面失活后已排队的晚到点击仍可能重新发起导航。已有的缩略图可见性、删除确认和移动动作防线未覆盖这条直接路由入口。

## 修复

`NoteCard` 单击在 `router.pushUrl()` 前先拒绝 `!pageActive`。长按菜单仍保持 `ResponseType.LongPress`，删除/移动、缩略图刷新和活动页正常打开语义不变。

## 证据与验证

- 新增 `ADR-0499-library-note-card-active-bound.md`、Harmony evidence 和专项 Replay。
- 专项 Replay `TOTAL=4 FAILED=0`，锁定入口守卫顺序、活动/失活模型及单击/长按分流。
- 相邻库页生命周期、查询 generation、文件夹、主题/搜索和笔记创建删除 Replay 通过。
- 全量 Desktop Replay：`REPLAY_FILES=422 PASSED=422 FAILED_FILES=0`（38.759 秒）。clean 2.908 秒、ohosTest HAP 9.343 秒、default HAP 48.457 秒均静态构建成功。

## 运行边界

未启动模拟器、虚拟机、真机或 Hypium。`T-042` 继续保持整个 Goal 的最后任务。
