# Phase 538：原版笔记卡片文件夹标签（folder chip）奇偶落地

日期：2026-09-22

## 选题

Phase 533 登记的残留差距：原版库笔记投影 `w09` 携带
`folderName`/`folderColor`，卡片 `cti.c` 渲染实心文件夹色
胶囊标签 + 录音 mic 图标；Harmony 已存 `folder.color` 但卡片
从未显示——视觉奇偶缺口。

## 原版现场重放（decompiled_1.0.3）

- `w09.java`：`f`=folderId、`g`=folderName、`h`=folderColor
  （可空 Integer）、`l`=recordings、`k`=shared 标志。
- `cti.java:94-186`：`str==null || num==null` → 不渲染；
  否则 `e5j.b(str,num)`；尾随行还有 `record_mic_outline`
  （l）与 `library_shared_outline`（k）。
- `e5j.java:34` → `i2j.java:28`：0.5dp 文件夹色描边 +
  `w3j.c` 实心填充（alpha=1.0）+ 名称文本。
- `kkf.java:1104`：int ARGB → Compose Color（`<<32` 打包）。

## Harmony 修改（ADR-0510）

- `NoteMeta` + `folderName`/`folderColor`；仓储
  `attachFolderProjection` 批量回查（一次 `IN` 查询），覆盖
  全部 8 个列表出口与 3 个单笔记路径；`FolderRepositoryImpl`
  的删除 committed 列表同样回填。
- VM：`noteWithFolder`/`noteWithFavorite` 携带字段；
  `publishCommittedNoteMove` 扩展为四参（页面从 folders 解析
  目标 name/color）；unfiled/文件夹删除置空。
- UI：`NoteCard` 尾随行——双非空条件渲染实心色胶囊
  （`#AARRGGBB` 归一化 + 亮度对比文本）+ `hasRecordings` 🎙
  （`recordings` a11y）。文件夹内部视图同样渲染，与原版一致。

## 验证

- 专项 replay `d02-original-note-folder-chip.mjs`：33 项断言
  全过（原版 w09/cti/e5j/i2j/kkf/w3j 锚点 + Harmony 锚点 +
  可执行可见性/颜色/移动投影模型）。
- 既有锚点合法更新：move-identity-bound 的 publish 签名。
- 全量 replay 433/433（仓根执行）。
- `note@default` 构建 BUILD SUCCESSFUL，0 个 ArkTS ERROR；
  `note@ohosTest` 见提交前验证。
- 未启动模拟器、虚拟机、真机或 Hypium。

## 残留差异（如实登记）

1. 省略 0.5dp 同色描边（实心填充下视觉冗余）。
2. 文本色用亮度对比（原版为样式默认色；Harmony 调色板含深色，
   对比计算保证可读）。
3. `🎙` 文本字形替代 `record_mic_outline`；shared 图标不移植
   （无共享模型）。
4. 原版文件夹色可空（无配色不显示 chip）；Harmony `folder.color`
   NOT NULL——filed 笔记恒显示 chip（Phase 533 已登记决策的
   延伸语义）。
