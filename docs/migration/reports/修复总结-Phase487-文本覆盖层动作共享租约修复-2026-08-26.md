# 修复总结 — Phase 487 文本覆盖层动作共享租约修复

## 问题

已打开文本编辑覆盖层已有响应式禁用，父页草稿、取消与直接提交边界也有防线；但覆盖层内的
输入、完成和取消回调缺少组件内第二层拒绝，租约切换竞态中仍可先改写本地状态。

## 修复

文本框变更与两个按钮回调先拒绝 `photoImportLeaseActive`，再执行原有草稿更新、提交或取消。
父页共享导入/历史守卫、宽度自适应预览、持久化事务、撤销语义和失败提示不变。

## 影响与验证

- 新增 ADR-0459，并在既有已打开文本 Harmony evidence 增量文件中补充本阶段记录。
- 扩展已打开文本共享租约专项 Replay 锁定三项新防线，当前 `TOTAL=13 FAILED=0`。
- ArkTS 目标无错误，仅既有警告与信息级提示。
- 相邻文本提交与文本取消 Replay 通过。
- 全量 Desktop Replay `REPLAY_FILES=421 PASSED=421 FAILED_FILES=0`
  （30.769 秒）；clean 3.251 秒、ohosTest HAP 9.892 秒、default HAP 30.076 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
