# Phase 180 修复总结：系统 Backup 快照可恢复发布

## 发现

系统 Backup 发布新快照时先删除旧 `nota-snapshot`，再重命名 staging。若重命名失败，上一份成功备份也会丢失。

## 修改

- `note/src/main/ets/notebackupability/NoteBackupAbility.ets`
- 新增 `ADR-0157-system-backup-snapshot-recoverable-publish.md`
- 新增 `d02-system-backup-snapshot-recoverable-publish.mjs`

旧快照现在先移动到 `nota-snapshot.previous`；staging 发布失败时恢复 previous，只有发布成功后才清理 previous。

## 验证

- Replay：`TOTAL=5 FAILED=0`
- Phase179 预检回归：`TOTAL=5 FAILED=0`
- `note@default assembleHap`：本轮未执行，仓库无 `hvigorw` 启动脚本；此前同一工作树记录为 `BUILD SUCCESSFUL`
- `note@ohosTest assembleHap`：本轮未执行，仓库无 `hvigorw` 启动脚本；此前同一工作树记录为 `BUILD SUCCESSFUL`
- 未启动设备、模拟器、虚拟机或 Hypium。

## 未闭环

真实文件系统重命名失败、进程中断和 CoreFileKit 生命周期仍需设备 fault injection。
