# Phase 583 — 原版全局 Hide/Reveal Tapes（2026-09-28）

## 原版证据

`decompiled_1.0.3/defpackage`：

- `o94.java` case14：胶带工具设置行按状态翻转 "Reveal Tapes" /
  "Hide Tapes" 标签与图标。
- `oh9` = OnToggleTapesRevealed → `ti9:360-380`：读取可见胶带 id 集，
  空集 no-op；全部已揭示→`np0` case5 `ys2.H` 差集隐藏，否则 `ys2.J`
  并集揭示。
- `yd9.j` = `NoteSessionState.revealedTape`：会话级 Set<elementId>，
  经 `gvb.k` 入渲染参数，**不持久化**。
- `i16:151-175`：元素 id 不在已揭示集合 → `ifeVar=null` 抑制胶带
  图案层（默认 `ife.STRIPES`）；笔迹本体经 `mwd.o` 烘焙路径照常绘制。
- `jg9.n`/`uf0` case4：菜单标签 = 任意可见胶带已揭示。
- `xtc.b`→`ej9` case20：点按命中胶带 → 揭示（入口在胶带工具路径）。

## Harmony 改动

- `StrokeCanvasPainter.ets`：新增 `revealedTapeIds: Set<string>`；
  `renderSpec.tapePattern` 存在且 id 不在集合中才渲染
  `renderTapePattern`——揭示态 = 去图案化，笔迹本体保留。
- `NoteCanvasView.ets`：会话级 `revealedTapeIds`（不持久化）+
  `@Prop tapeToggleSignal` watcher；`toggleTapesReveal()` 复刻
  np0 case5 全量并/差集 + `ti9:362` 空集 no-op + 重绘；
  `emitTapeRevealState()` 发出 `(tapeCount, anyRevealed)`，页面
  加载/切页后刷新。
- `NotePage.ets`：`@State tapeToggleSignal/pageTapeCount/
  anyTapeRevealed`；⋮ 菜单在 `pageTapeCount > 0` 时提供
  Hide/Reveal 行，标签 `anyRevealed ? hide_tapes : reveal_tapes`。
- 字符串：`tape_tool`/`hide_tapes`/`reveal_tapes`（EN + zh_CN）。

## 影响

导入 `.note` 中的胶带笔画现可按原版语义整组隐藏/揭示图案层，查看
被遮盖内容；揭示集合会话级、不写库，与原版一致。

## 验证

- 专项 `d02-original-tape-hide-reveal.mjs`：19/19。
- `note@default` 构建 0 错误。

## 范围外（fail-closed 记录）

- wet-reveal 揭示动画层（`l0f.A`/`p0f`）：最终派发函数未反编译成功。
- 点按揭示（`xtc`）：Harmony 尚无胶带工具命中入口。
- 菜单表面位于编辑器 ⋮ 菜单而非胶带工具设置行（REVIEW 工具行未建）。
