# Phase 194 修复总结：创建文件夹顺序归一化

## 发现

移动/删除已归一化兄弟序号，但创建入口仍直接追加到历史最大值；旧数据库存在空洞时，
创建后顺序仍不连续。

## 修改

- `FolderRepositoryImpl.ets` 创建事务插入后复用兄弟序号归一化
- 新增 ADR、replay 和本报告

## 验证

- Replay：`TOTAL=5 FAILED=0`
- HAP/设备测试：本轮未执行
