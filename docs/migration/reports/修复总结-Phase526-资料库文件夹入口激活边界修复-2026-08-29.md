# 修复总结 — Phase 526 资料库文件夹入口激活边界修复

日期：2026-08-29

## 问题

补审发现文件夹领域多个入口只检查 `folderBusy` 或仓库存在，未先检查 `pageActive`。页面失活后已打开菜单、对话框或拖拽事件仍可能改写对话框/树状态、进入 busy 并启动无意义查询或持久化，虽然后续结果会被生命周期 guard 丢弃。

## 修复

为新建/重命名对话框、删除确认、删除/移动文件夹、移动笔记、按索引重排、拖拽捕获、展开文件夹和打开 compact drawer 统一增加 `pageActive` 首行准入；关闭 drawer 保持可用，便于清理 UI。

## 证据与验证

- 新增 `ADR-0498-library-folder-entry-active-bound.md`。
- 在文件夹 mutation lifecycle evidence 追加 Phase 526 增量。
- 扩展 `d02-library-folder-mutations-lifecycle-bound.mjs` 至 `TOTAL=18 FAILED=0`，锁定 10 个入口与失活模型。
- 文件夹选择、查询 generation、初始化、拖放、主题/搜索和笔记创建删除相邻 Replay 通过；ArkTS 无新增错误。
- 全量 Desktop Replay：`REPLAY_FILES=421 PASSED=421 FAILED_FILES=0`（33.836 秒）。clean 2.951 秒、ohosTest HAP 9.097 秒、default HAP 27.608 秒均静态构建成功。

## 运行边界

未启动模拟器、虚拟机、真机或 Hypium。`T-042` 继续保持整个 Goal 的最后任务。
