# Phase 169 修复总结：备份恢复全对象校验门

## 发现

云恢复在批次内部分对象下载或 hash 校验失败时，仍会逐篇导入其余对象。这把 manifest 声明的完整快照拆成残缺恢复，并修改本地数据。

## 修改

- `note/src/main/ets/ui/settings/BackupPage.ets`
- 新增 `ADR-0146-backup-restore-verification-gate.md`
- 新增 `d02-backup-restore-verification-gate.mjs`

现在必须全部对象通过下载、大小和 hash 校验后才进入本地导入循环；任一对象失败时恢复数为零，本地不写入该批次。

## 验证

- Replay：`TOTAL=5 FAILED=0`
- `note@default assembleHap`：本轮未执行，仓库无 `hvigorw` 启动脚本；此前同一工作树记录为 `BUILD SUCCESSFUL`
- `note@ohosTest assembleHap`：本轮未执行，仓库无 `hvigorw` 启动脚本；此前同一工作树记录为 `BUILD SUCCESSFUL`
- 未启动设备、模拟器、虚拟机或 Hypium。

## 未闭环

全部对象校验通过后，多篇笔记逐篇落库仍不是单一数据库事务；中途导入失败时可能已有前篇成功。该第二阶段原子性仍需后续设计和修复。
