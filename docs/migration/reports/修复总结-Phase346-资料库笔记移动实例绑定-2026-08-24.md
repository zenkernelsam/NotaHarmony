# Phase 346 修复总结：资料库笔记移动实例绑定

日期：2026-08-24（Asia/Shanghai）

## 缺陷与修复

- `moveNote()` 的迟到成功只校验 lifecycle generation 和 `pageActive`，未校验捕获的 ViewModel 是否仍是当前实例；页面重建后旧上下文仍可修改旧 ViewModel、发布 notes 并触发缩略图刷新。
- 成功 guard 现在显式增加 `this.viewModel === vm`；陈旧成功直接返回，durable 移动数据保持权威。当前上下文的权威刷新和 request-generation 保护不变。
- 既有 folder mutation Replay 的源码契约保留；专项 Replay 同时验证新身份检查。

## 产物与验证

- ADR：`docs/migration/adr/ADR-0323-note-move-identity-bound.md`
- Evidence：`docs/migration/evidence/library-note-move-identity-bound-harmony-2026-08-24.md`
- Replay：`docs/migration/replays/d02-library-note-move-identity-bound.mjs`
- 全量 Desktop Replay 与双 HAP 静态构建结果见提交前补录。
- 未启动模拟器、虚拟机、真机或 Hypium，`T-042` 继续 Goal 最后一项。
## 提交前验证补录

- 全量 Desktop Replay：`REPLAY_FILES=313 PASSED=313 FAILED_FILES=0`。
- 双 HAP 静态构建成功：ohosTest 13.398 秒，主 HAP 3.441 秒，合计约 19 秒。未签名警告为既有配置状态。