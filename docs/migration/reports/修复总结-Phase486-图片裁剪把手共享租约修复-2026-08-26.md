# 修复总结 — Phase 486 图片裁剪把手共享租约修复

## 问题

图片裁剪按钮已有响应式禁用，父页四个回调也有双层守卫；但把手 `onActionUpdate()` 直接发布
移动增量，缺少组件内租约防线。导入收口窗口内，竞态手势仍可能改写裁剪草稿。

## 修复

把手拖动更新回调先拒绝 `photoImportLeaseActive`，再执行原有增量计算与父页回调。关闭、重置、
确认、选区几何、确认事务、历史语义和销毁防护全部保留。

## 影响与验证

- 新增 ADR-0458、Harmony evidence 增量段和本中文 Report。
- 扩展已打开裁剪共享租约专项 Replay 锁定新防线，当前 `TOTAL=11 FAILED=0`。
- ArkTS 目标无错误，仅既有警告与信息级提示。
- 相邻图片裁剪共享租约、本地图片裁剪出界 Replay 通过。
- 全量 Desktop Replay `REPLAY_FILES=421 PASSED=421 FAILED_FILES=0`
  （32.457 秒）；clean 3.446 秒、ohosTest HAP 9.929 秒、default HAP 30.383 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
