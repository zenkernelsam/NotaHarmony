# 修复总结 — Phase 522 Math 覆层忙碌回调边界修复

日期：2026-08-26

## 问题

继续补审发现 `MathEditorOverlay` 的取消和完成按钮虽然响应式禁用，但直接回调只拒绝共享照片导入
租约；`busy=true` 时晚到点击仍可进入回调，依赖父级 `historyBusy` 和确认方法内部守卫兜底。

## 决策

两个按钮回调统一改为先拒绝 `this.busy || this.photoImportLeaseActive`。输入变更、Done 四态模型
门禁、父级历史串行化、保存失败保留草稿、导航销毁清理和正常语义不变。

## 证据与验证

- 新增 `ADR-0494-math-overlay-busy-callback-bound.md`。
- 在既有 open-math 共享租约 evidence 追加 Phase 522 增量段。
- 扩展 `d02-open-math-shared-ingress-lease-bound.mjs` 至 15/15：要求恰好两处组合早退，
  并显式禁止旧的“仅共享租约”按钮回调模式。

## 运行边界

未启动模拟器、虚拟机、真机或 Hypium。T-042 继续保持整个 Goal 的最后任务。
