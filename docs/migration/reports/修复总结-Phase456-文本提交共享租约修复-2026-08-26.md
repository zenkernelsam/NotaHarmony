# 修复总结 — Phase 456 文本提交共享租约修复

## 问题

照片导入前打开的文本编辑会话，仍可在异步导入释放 `historyBusy` 后、共享租约收口前提交。
该提交会替换或新增文本块、维护元素顺序、推入撤销栈并持久化，与照片插入结果形成并发窗口。

## 修复

统一提交入口在内部历史门禁前拒绝 `photoImportBusy` 并返回失败，保留草稿等待导入收口后
再次确认。空新建文本取消、CRDT 预览和页上下文校验不变。

## 影响与验证

- 新增 ADR-0428、Harmony evidence 和中文 Report。
- 新增专项 Replay `d02-text-commit-shared-lease-bound.mjs`，当前 `TOTAL=7 FAILED=0`。
- 同步扩展 `d02-text-final-commit-stale-bound.mjs` 的共享租约断言。
- 全量 Desktop Replay `REPLAY_FILES=397 PASSED=397 FAILED_FILES=0`（29.027 秒）；
  ArkTS 目标文件无错误，仅既有 unused/deprecation 信息；clean 2.986 秒、ohosTest HAP
  31.345 秒、default HAP 14.285 秒静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
