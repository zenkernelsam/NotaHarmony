# 修复总结 — Phase 457 数学确认共享租约修复

## 问题

照片导入前打开的数学会话，可在异步导入释放 `historyBusy` 后、共享租约收口前确认。编辑
会替换 Math，插入会新增 Math 并更新元素顺序；二者都会推入撤销栈并与照片结果竞争。

## 修复

编辑启动、编辑确认和插入确认统一在内部历史门禁前拒绝共享导入租约。既有会话保留到导入
完成后重新确认；CRDT 预览、页上下文防御和失败恢复不变。

## 影响与验证

- 新增 ADR-0429、Harmony evidence 和中文 Report。
- 新增专项 Replay `d02-math-commit-shared-lease-bound.mjs`，当前 `TOTAL=9 FAILED=0`。
- 全量 Desktop Replay `REPLAY_FILES=398 PASSED=398 FAILED_FILES=0`（28.584 秒）；
  ArkTS 目标文件无错误，仅既有 unused/deprecation 信息；clean 3.130 秒、ohosTest HAP
  31.771 秒、default HAP 15.197 秒静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
