# 修复总结 — Phase 517 标题加载边界修复

## 问题

标题输入、回车和失焦回调可在页面加载失败或仍在加载时触发；晚到事件会更新草稿、退出编辑态并排队
标题保存，依赖内部页校验抛错。

## 修复

三个回调先拒绝共享导入租约、`pageLoadFailed` 或 `pageLoading`，再执行原草稿更新或保存。序列化队列、
幂等发布、失败恢复、销毁守卫、历史语义和点击入口全部保留。

## 影响与验证

- 新增 ADR-0489，并在既有标题共享租约 evidence 文件追加本阶段增量段。
- 扩展标题共享租约 Replay 至 15 项，覆盖三项回调的入口防线，当前通过。
- ArkTS 目标无错误，仅既有警告与信息级提示。
- 相邻顶栏共享租约 Replay 已同步扩展防线断言。全量 Desktop Replay
  `REPLAY_FILES=421 PASSED=421 FAILED_FILES=0`（34.622 秒）；clean 3.157 秒、ohosTest HAP 10.108
  秒、default HAP 31.074 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
