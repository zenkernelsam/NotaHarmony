# Phase 344 修复总结：局部擦除与图片插入陈旧绑定

日期：2026-08-24（Asia/Shanghai）

## 缺陷与修复

- `commitOriginalPartialErase()` 的迟到成功仍可完成预览簿记、push undo 并发布通知；`insertOriginalPhotos()` 的迟到成功也可 push undo、修改元素数组并发布通知。原 guard 只覆盖部分 UI 安装。
- 两个路径现在都在 durable 成功后、任何本地效果前检查 `isHistoryPageContextCurrent(generation, pageId)`。陈旧成功只记录 durable 历史可用；Photo 路径仍返回已提交数量供入口状态使用，但不触达本页 undo/UI。
- 失败用户提示继续仅限当前上下文；`finally` 无条件复位 busy/preview 状态。

## 产物与验证

- ADR：`docs/migration/adr/ADR-0321-partial-erase-photo-stale-bound.md`
- Evidence：`docs/migration/evidence/partial-erase-photo-stale-bound-harmony-2026-08-24.md`
- Replay：`docs/migration/replays/d02-partial-erase-photo-stale-bound.mjs`
- 全量 Desktop Replay 与双 HAP 静态构建结果见提交前补录。
- 未启动模拟器、虚拟机、真机或 Hypium，`T-042` 继续 Goal 最后一项。
## 提交前验证补录

- 全量 Desktop Replay：`REPLAY_FILES=311 PASSED=311 FAILED_FILES=0`。
- 双 HAP 静态构建成功：ohosTest 14.548 秒，主 HAP 3.283 秒，合计约 20 秒。未签名警告为既有配置状态。