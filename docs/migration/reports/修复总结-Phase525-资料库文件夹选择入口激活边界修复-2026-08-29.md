# 修复总结 — Phase 525 资料库文件夹选择入口激活边界修复

日期：2026-08-29

## 问题

Phase 354 已保护 `selectFolder()` 的异步续体，但入口没有先检查 `pageActive`。失活页面的晚到菜单/抽屉点击仍会改写 `currentFolderId`、递增请求代数并启动 ViewModel 查询，结果随后才被生命周期 guard 丢弃。

## 修复

`selectFolder()` 现在第一行统一拒绝 `!pageActive || viewModel === null || folderBusy`，再捕获查询上下文并修改当前文件夹。成功/失败续体、缩略图刷新、抽屉关闭和忙态清理语义不变。

## 证据与验证

- 新增 `ADR-0497-folder-selection-active-entry-bound.md`。
- 在既有文件夹选择生命周期 evidence 追加 Phase 525 增量。
- 扩展 `d02-library-folder-selection-lifecycle-bound.mjs` 至 `TOTAL=11 FAILED=0`，锁定入口守卫顺序与失活/活动模型。
- 相邻库页查询 generation、文件夹 mutation、拖放、初始化和主题/搜索 Replay 通过；ArkTS 无新增错误。

## 运行边界

未启动模拟器、虚拟机、真机或 Hypium。`T-042` 继续保持整个 Goal 的最后任务。
