# Phase 585 — 原版胶带图案选择器（2026-09-28）

## 原版证据

`decompiled_1.0.3/defpackage`：

- `mh9` = OnShowTapePatterns → `ti9:352` → `bi9` variant3 → `t7f`
  底栏内容。
- `lfe.a`：9 图案按 `ife` 声明序 [STRIPES…CHECKERS]，工具色小样 +
  当前选中态；点击 `ej9(29)` → `i31` case26 → `nh9(序数)`。
- `ife.I` = 图案序数字节（与 TapePattern 0..8 一一对应）。
- `nh9` → `yh9`(i3=1)：`g5f.h` 复制工具态替换 tapePattern，`ti9.q`
  应用 + 持久化（tool_state.tape_pattern）。
- `lfe.b`：工具设置页图案行预览当前图案小样。

## Harmony 改动

- 新增 `ui/editor/TapePatternPicker.ets`：`TAPE_PATTERN_ORDER` 同序
  9 小样网格；`TapePatternSwatch` 以合成笔画经 `renderTapePattern`
  实绘（带体色 + 图案层，PLAIN 纯色带）。
- `ToolboxSettingsDialog.ets`：REVIEW 行 ⋯ 菜单增 `tape_patterns`
  项 → `tapePatternToolId` 切换对话内容为选择面板。
- `EditorViewModel.ets`：`setToolTapePattern(toolId, pattern)` ——
  限 REVIEW 行 + 序数校验，`updateState` 写 tapePattern 并
  `saveToolState` 持久化；激活行经 `applyActiveState` 即时生效。
- 字符串：`tape_patterns`（EN + zh_CN）。

## 影响

胶带图案可按原版语义在 9 图案间切换，选择持久化到
tool_state.tape_pattern，新画笔画即时使用所选图案；小样复用真实
渲染管线，所见即笔迹。

## 验证

- 专项 `d02-original-tape-pattern-picker.mjs`：25/25。
- `note@default` 构建 0 错误。

## 范围外

表面偏差：原版入口在胶带工具自身设置页（Harmony 无单工具页，置于
工具箱设置 REVIEW 行菜单）；点按揭示（xtc）仍待后续。
