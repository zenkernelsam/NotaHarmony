# 修复总结 — Phase 523 页面设置颜色事件忙碌边界修复

日期：2026-08-26

## 问题

补审发现 `PageSettingsPanel` 的自定义颜色 H/S/V 滑杆只拒绝共享照片导入租约。忙碌、共享纸库加载或间距保存期间的晚到事件仍可改写 HSV 控件，并提交背景颜色草稿。

## 修复

`updateCustomHue()`、`updateCustomSaturation()` 与 `updateCustomValue()` 统一先拒绝 `busy || photoImportLeaseActive || sharedBusy || spacingSaveBusy`，然后才执行限幅和 `stageCustomColor()`。其他设置入口、共享模板/收藏/间距路径及正常语义保持不变。

## 证据与验证

- 新增 `ADR-0495-page-settings-color-event-bound.md`。
- 在页面设置共享租约 evidence 中追加 Phase 523 增量。
- `d02-page-settings-shared-ingress-lease-bound.mjs` 扩展至 `TOTAL=25 FAILED=0`，锁定恰好三处组合守卫并禁止旧模式。
- 相邻纸张设置、销毁、收藏刷新与颜色纹理 Replay 均通过；ArkTS 检查无新增错误。

## 运行边界

本阶段只执行静态源检查与 Desktop Replay；未启动模拟器、虚拟机、真机或 Hypium。`T-042` 继续保持整个 Goal 的最后任务。
