# Phase 374 修复总结：通用保存入队失败上下文绑定

日期：2026-08-24（Asia/Shanghai）

## 缺陷与修复

- 补审发现 `persist()` 的 `queueSaveElements()` 同步失败无条件弹保存失败；销毁或换代后的迟到异常可触达旧 Canvas。
  同一函数的 deferred flush 失败已有统一门禁，两处不一致。
- 现在同步失败复用 `isHistoryPageContextCurrent` 联合校验活动页、同代、页面身份与加载健康；陈旧态直接返回，活动页语义不变。

## 产物与验证

- ADR：docs/migration/adr/ADR-0351-persist-enqueue-failure-context-bound.md
- Evidence：docs/migration/evidence/persist-enqueue-failure-context-bound-harmony-2026-08-24.md
- Replay：docs/migration/replays/d02-persist-enqueue-failure-context-bound.mjs
- 验证补录：专项 Replay 4/4；相邻普通粘贴入队 Replay 4/4、局部擦除上下文 Replay 5/5；全量 Desktop Replay
  `REPLAY_FILES=341 PASSED=341 FAILED_FILES=0`；双 HAP 静态构建成功（ohosTest 10.208 秒、主 HAP
  54.078 秒）。
- 未启动模拟器、虚拟机、真机或 Hypium，T-042 继续 Goal 最后一项。
