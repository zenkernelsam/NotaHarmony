# Phase 144 修复总结：原版本地复选框 Type-28 出站

## 原版对照与问题

- 原版 `fm7.n()` 通过 `toggleCheckbox` bridge 进入 `wh`：只允许 CHECK_BOX document block，按
  Unicode 字符位置调用 `t3c.w()` 得到稳定 SeqId，再把当前值取反。
- `gv0.o()` 将 location/boolean 交给 `u5j.J()`，后者写 type-28 `mqf` 的 TextField、location 和
  isChecked；false 是 FlatBuffer 缺省值，可以省略字段但仍代表明确的未勾选更新。
- Harmony 此前只有远端 type-28 reducer。若本地直接修改 `paragraphStyleRuns`，保存器无法识别，
  会绕过独立 checkbox CRDT、私有同步和可靠持久历史。

## 已完成修复

- 新增 canonical type-28 writer，true/省略 false 均经生产 decoder 往返；同时移除 reducer 对
  `StrokePersistence` 的反向 clone 依赖，改用模型 clone，避免新 writer 接线形成模块环。
- 新增纯 checkbox toggle/planner：只接受段首 decorator=3，按 code point 处理 Unicode，映射
  当前可见字符的稳定 SeqId；正文、字符样式、几何或多处状态同时变化全部拒绝。
- 普通保存和 grouped Undo/Redo 在正文编辑前识别 checkbox mutation；写前重新读取字符树、style
  operations 与 checkbox registers，要求 before snapshot 与 reducer authority byte-exact 对齐。
- 生产路径分配独立 operation identity，写 upload-immediate type-28，经现有 reducer 落库并核对
  canonical after snapshot，再追加正常 history companion。新增 OpType 与 PersistentHistory companion。
- 新增 ArkTS fixture、ADR-0121、`d02-local-update-checkbox.mjs`，并升级既有入站 replay。

## 验证与边界

- 专项 replay 输出：
  `localUpdateCheckbox=original-toggle-bridge-stable-location-type28-default-false-authority-guard-history-upload`。
- 全量桌面 replay 为 `TOTAL=130 FAILED=0`；`hvigorw clean` 后严格串行构建
  `note@ohosTest` 与 `note@default`，两套 HAP 均为 `BUILD SUCCESSFUL`。未启动模拟器、虚拟机、
  真机或 Hypium。
- 本阶段完成协议与持久化，不冒充 UI 完成。当前 TextArea 尚未暴露原版 marker 点击区域；下一阶段
  需用与 Canvas 排版一致的 hit test 接入 REPLACE_ELEMENT Undo/Redo，Goal 保持 active。
