# Phase 153 修复总结：文件夹笔记归属原子移动

## 修复

`FolderRepositoryImpl.moveNoteToFolder()` 现在与文件夹新建、重命名、移动、删除共用写互斥，并在一个数据库事务中完成目标校验和 `note_meta.folder_id` 更新。并发删除目标文件夹、目标不存在、笔记不存在或写入失败均不会留下半状态。

## 验证

`d02-folder-move-atomicity.mjs`：`TOTAL=4 FAILED=0`。

本阶段未启动设备、模拟器、虚拟机或 Hypium；窄屏库页和系统数据库并发压力仍需后续运行态验收。
