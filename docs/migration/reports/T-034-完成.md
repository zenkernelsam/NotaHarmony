# T-034 完成报告

> 工人: 编码助手 | 日期: 2026-08-04 | 状态: 待指挥官复核

## 改了什么

1. `note/src/main/ets/rendering/CanvasViewport.ets`（新建）：视口状态管理
   - `screenToCanvas` / `canvasToScreen`（屏幕↔画布坐标映射）
   - `zoomAt`（以屏幕点为中心缩放，保持该点内容不动）/ `panBy` / `setZoom` / `setScroll` / `reset` / `clamp`
   - zoom 范围已由 Phase 259 按原版 1.0.3 更正为 [0.25, 10.0]；滚动边界 MVP 不限制（自由平移）
2. `note/src/main/ets/ui/editor/NoteCanvasView.ets`：
   - **渲染变换**：renderFrame 中 `translate(scrollX, scrollY) + scale(zoom)` 后绘制背景/已完成层/文本框；背景铺满视口可见区（w/zoom, h/zoom）
   - **输入映射**：所有输入点（书写/擦除/选区/双击/拖拽）经 `screenToCanvas` 逆映射（toRawPointerEvent 内统一转换）；选区 overlay 边框用 `canvasToScreen` 转屏幕坐标
   - **手势**：`GestureGroup(Exclusive, [Parallel(PinchGesture(2), PanGesture(2))])`——双指捏合缩放（以两指中心为锚，增量换算）+ 双指平移；onTouch 中 `touches.length > 1` 忽略（单指书写不受干扰）
   - **缩放控制条**：右下角 `[-][百分比][+][适应宽度]`（步进 0.25；适应宽度按页面宽 800 画布坐标铺满 85%）
   - **持久化**：缩放/滚动变化 → `scheduleViewportSave`（防抖 500ms）→ `saveViewState`；loadNoteData 恢复 zoom/scroll
3. `note/src/main/ets/rendering/PaperRenderer.ets`：`renderBackground` 加 zoom 参数，线宽/点半径除以 zoom（保持视觉 1px）

## 验收结果（模拟器实测）

| 验收项 | 结果 |
|--------|------|
| check_ets_files 零错误 | ✅（仅既有弃用 Information） |
| build_project 通过 | ✅ BUILD SUCCESSFUL |
| 缩放按钮 +/- 可用 | ✅ 100% → 125%（点击 + 后 UI dump 确认） |
| 百分比显示正确 | ✅ 125% |
| 退出重进 zoom 恢复 | ✅ 退出重进后控制条显示 125% |
| 缩放后书写位置准确 | ✅ 125% 下画一笔，深色像素集中在指尖轨迹带（y≈800-850 段 10396 vs 其余段 1300-4900）；中心锚点不动（zoomAt 语义验证） |
| 双指捏合/平移 | ⚠️ 代码实现（PinchGesture + PanGesture(2) + 双指忽略），模拟器无法模拟双指手势，待真机验证 |
| 模拟器不崩溃 | ✅ 多次启动/操作无崩溃 |

## 遗留问题

- 双指手势（捏合/平移）需真机验证（模拟器 MCP 无双指能力）。
- 滚动边界未钳制（MVP 自由平移）；适应宽度为固定基准简化（800 画布坐标）。
- 缩放控制条位置在画布内偏上（Stack 布局约束），T-040 回归时可微调。

## 2026-08-17 Phase 259 原版范围与按钮步进更正

- 原报告的 4×上限来自早期 T-034 自定规格，不是原版事实。原版 `t0g/h3a/v0g` 明确使用 0.25×～10×；
  `CanvasViewport`、适宽和 repository 持久化现共享该范围。
- 旧 `zoomStep(0.25)` 实际调用倍率 `1.25`，连续点击会得到 100%→125%→156%→195%。现新增加法
  `stepZoomAt()`，恢复任务本身规定的 100%→125%→150%→175%，并保持视口中心锚点。
- 原版键盘 zoom 使用 1.2/0.8333333 倍率；本报告不把 Harmony 右下角按钮的 0.25 步进伪称为原版行为。
- 25/100/400/1000% 的坐标往返和 0.25×→10×锚点已进入 ArkTS fixture；1000% 像素、缓存和手势仍待设备验收。
