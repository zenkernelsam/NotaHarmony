# Phase 179 修复总结：系统 Backup 恢复全量预检

## 发现

系统 Backup 恢复原先在同一个循环中校验并复制文件。批次后部出现损坏条目时，前部文件已经写回，失败结果会留下部分恢复状态。

## 修改

- `note/src/main/ets/notebackupability/NoteBackupAbility.ets`
- 新增 `ADR-0156-system-backup-restore-preflight.md`
- 新增 `d02-system-backup-restore-preflight.mjs`

恢复现在分为两步：先检查全部源文件存在且大小精确匹配，再进入 `restoring` 阶段执行复制。任何预检失败都不会写入目标目录。

## 验证

- Replay：`TOTAL=5 FAILED=0`
- Phase177 数值边界回归：`TOTAL=5 FAILED=0`
- `note@default assembleHap`：本轮未执行，仓库无 `hvigorw` 启动脚本；此前同一工作树记录为 `BUILD SUCCESSFUL`
- `note@ohosTest assembleHap`：本轮未执行，仓库无 `hvigorw` 启动脚本；此前同一工作树记录为 `BUILD SUCCESSFUL`
- 未启动设备、模拟器、虚拟机或 Hypium。

## 未闭环

复制阶段中途文件系统写失败仍可能留下部分恢复；这需要 CoreFileKit/设备集成测试或临时目录交换策略进一步确认。
