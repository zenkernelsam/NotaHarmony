# Phase 160 修复总结：Note 导出精确字节视图

## 发现

导出到临时文件和 picker 目标文件时直接写入 `Uint8Array.buffer`。带偏移或共享更大底层 buffer 的视图可能写出额外字节。

## 修改

- `note/src/main/ets/data/NoteExporter.ets`
- 新增 `ADR-0137-note-export-exact-byte-view.md`
- 新增 `d02-note-export-exact-byte-view.mjs`

增加 `exactArrayBuffer()`，两个写入点都只写有效字节范围。

## 验证

- Replay：`TOTAL=6 FAILED=0`
- `note@default assembleHap`：`BUILD SUCCESSFUL`
- `note@ohosTest assembleHap`：`BUILD SUCCESSFUL`
- 未启动设备、模拟器、虚拟机或 Hypium。

## 未闭环

系统 picker 真实交互、跨平台文件 provider 和完整导入导出设备回归仍待设备阶段。
