# Phase 549：原版库分区与文件夹空态

日期：2026-09-22

## 选题

原版 `hf0`/`yw3` 为每个分区与文件夹渲染专属空态（图标+可选标题+
正文）；Harmony 仅 FAVORITES 专属，其余一律 `no_matching_notes`。

## 原版现场重放

- `hf0` 分派：ALL_NOTES→"Let's get started!"+引导；RECENT/
  FAVORITES/UNFILED→各自 title+body+xxlrg 图标；空文件夹→
  `empty_folder_body`（无标题）；有子项的空文件夹→计数正文
  （复数 plurals + HTML `<b>`）。

## Harmony 落地

- `emptyStateIcon/HasTitle/Title/Body` 按 folder+section 分派；
  文件夹无标题行；有子文件夹时渲染计数正文（笔记数恒 0）。
- 搜索激活时保留 `no_matching_notes`。
- 9 条新双语言字符串（EN 与原版一致，去 `<b>` 标签）。

## 验证

- `d02-original-library-empty-states.mjs`：23 断言全过。
- 全套 replay：**444 PASS / 0 FAIL**；`note@default` +
  `note@ohosTest` BUILD SUCCESSFUL。

## 登记差异

- 字形图标替代矢量；HTML 加粗省略；复数经双正文变体（SDK 无同步
  plural API）；SHARED 空态不移植。
