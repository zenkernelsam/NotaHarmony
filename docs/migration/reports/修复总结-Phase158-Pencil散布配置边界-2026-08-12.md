# Phase 158 修复总结：Pencil 散布配置边界

## 发现

`PencilSplatGenerator` 会直接接收可选配置。非法 `NaN`、Infinity、零步长、负细分数或非整数预算可能使 splat 细分、LCG 或预算比较进入不确定状态。

## 修改

- `note/src/main/ets/core/algorithm/PencilSplatGenerator.ets`
- 新增 `ADR-0135-pencil-splat-config-boundaries.md`
- 新增 `d02-pencil-splat-config-boundaries.mjs`

构造函数现在对配置做有限性、符号和整数边界检查；非法值回退既有默认值。原版 `xaa` 的散布公式、随机顺序、压力/倾角语义和 splat 输出未改动。

## 验证

- `d02-pencil-splat-config-boundaries.mjs`：`TOTAL=10 FAILED=0`
- `note@default assembleHap`：`BUILD SUCCESSFUL`
- `note@ohosTest assembleHap`：`BUILD SUCCESSFUL`
- 未启动设备、模拟器、虚拟机或 Hypium。

## 未闭环

真实 PenKit 压力/倾角/方位角输入、原版纹理像素、设备性能和完整 NOTE_BUNDLE payload 仍待后续阶段；本阶段不宣称 Pencil 与原版运行态完全等价。
