# Phase 542：原版跳转到页（Jump to Page）

日期：2026-09-22

## 选题

原版页指示器 `page_indicator` 可点击：经 Compose `m18.K` clickable
修饰链打开 `jump_to_title` 对话框，输入页码后 Go 仅在
`1 <= n <= pageCount` 时可用。Harmony 为纯文本指示器，缺跳转入口。

## 原版现场重放

- `n8.java` case 20：`tl7.T(page_indicator, {i2, list.size()})` 文本经
  `dac.m(..., m18.K(pd8VarJ, false, null, l5cVar, g89(gl8Var,25), 11), ...)`
  包裹——clickable 回调 `g89` 置 dialog 可见态。
- `ke1.java:164/174`：`jump_to_title` 标题 + `jump_to_page_label` 输入标签。
- `n8.java` case 21：`svd.p0(input)` 解析为 `Integer`，Go `enabled =
  numP0 != null && 1 <= n <= i2(pageCount)`。

## Harmony 落地

- 指示器 `Text` 加 `onClick` → `jumpDialog.open()`；fail-closed
  守卫（busy/lease）与 `.enabled(!busy && !lease)` 视觉同步。
- `JumpToPageDialog`（@CustomDialog）：标题 + Number 输入（初始=当前页）
  + Cancel/Go；`parseInt`+整串比对复刻 `svd.p0`；Go enabled iff
  `1 <= n <= pageCount`，点击二次校验后回调 `onGo(target-1)`。
- `NotePage.onJumpToPage`：与 prev/next 相同的守卫集（loading/busy/
  history/lease + 边界）后赋 `currentPageIndex`。
- `jump_to_title`/`jump_to_page_label`/`jump_to_go` 双语字符串。

## 验证

- `d02-original-jump-to-page.mjs`：21 断言全过（原版 clickable 修饰链 +
  `svd.p0` 范围校验 + Harmony 锚点 + 可执行 Go-enable 模型）。
- `d02-page-bar-shared-lease-bound` 守卫计数锚点 9→10 合法演进。
- 全套 replay：**437 PASS / 0 FAIL**；`note@default` +
  `note@ohosTest` BUILD SUCCESSFUL。

## 登记差异

- ArkUI `CustomDialogController` 替代原版 Compose `AlertDialog`；
  `accessibilityText` 标注指示器可点性。
