# Phase 547：原版库网格/列表视图切换

日期：2026-09-22

## 选题

原版库有 `ie7` 视图模式（GRID/LIST，z97 持久化默认 GRID），工具条
图标按钮切换、图标描绘目标模式；`yj9` 按模式分派网格卡片/列表行。
Harmony 仅网格。

## 原版现场重放

- `inh.d`：图标按钮派发相反 ie7；`zy7` case 20 图标=目标模式
  （GRID 态显示 list_bullet、LIST 态显示 grid_view）。
- `yj9`：`ie7.ordinal()` 分派 `b5j.b`（网格卡）/`m5j.b`（紧凑行）。

## Harmony 落地

- `@State listView` + `library_view_mode` pref（默认 GRID）持久化。
- 头部按钮：GRID 态 `☰`（目标 list）、LIST 态 `⊞`（目标 grid）；
  紧凑菜单同义切换项。
- `NoteListRow`：48×64 缩略图 + 标题/日期/文件夹 chip/♥/🎙 列，
  点击打开、长按菜单、懒加载缩略图与卡片一致。

## 验证

- `d02-original-library-view-mode.mjs`：13 断言全过。
- 全套 replay：**442 PASS / 0 FAIL**；`note@default` +
  `note@ohosTest` BUILD SUCCESSFUL。

## 登记差异

- ⊞/☰ 字形替代矢量图标；行卡为简化紧凑行（无多选装饰）。
