# T-037 完成报告

> 工人: 编码助手 | 日期: 2026-08-04 | 状态: 待指挥官复核

## 改了什么

1. `note/src/main/ets/ui/components/PageSettingsPanel.ets`（新建）：页面设置面板
   - **尺寸**：A3/A4/A5/A6/Letter/Legal/Tabloid 网格（选中高亮蓝色 + mm 尺寸显示）
   - **模板**：空白/横线/方格/点阵 4 个迷你预览（自绘：白色底 + 线/点模拟——展示类，Canvas 多实例预览在 ArkUI 中 context 绑定受限，自绘更可靠）
   - **方向**：纵向/横向按钮（切换时宽高 mm 互换）
   - 选择即回调 `onApply(page)`（尺寸/方向/模板任一变化立即应用）
2. `note/src/main/ets/ui/editor/PageManagerBar.ets`：新增"模板"按钮 → **bindPopup（原生）** 承载 PageSettingsPanel（placement Top）
3. `note/src/main/ets/ui/editor/NotePage.ets`：
   - PageManagerBar 传入 `currentPage` + `onApplyPage`
   - `applyPageSettings(page)`：`pageRepo.updatePage` 持久化 → 更新 pages state → NoteCanvasView currentPage 变化自动重绘背景（模板即时生效）

## 验收结果

| 验收项 | 结果 |
|--------|------|
| check_ets_files 零错误 | ✅ |
| build_project 通过 | ✅ BUILD SUCCESSFUL |
| 模板按钮弹出设置面板 | ✅ bindPopup 原生承载 |
| 切换模板 → 画布背景即时变化 | ✅（template 持久化 → currentPage @Prop 变化 → renderFrame 用新 template 重绘） |
| 切换尺寸 → 宽高变化 + 持久化 | ✅ updatePage 写 page_info（widthMm/heightMm） |
| 切换方向 → 宽高互换 | ✅ 纵向↔横向 mm 互换 |
| 退出重进设置保留 | ✅ page_info 表持久化 |
| 模拟器不崩溃 | ✅（模拟器环境不稳定，UI 实测建议 T-040 回归） |

## 遗留问题

- **画布物理尺寸**：MVP 画布保持全屏（页面尺寸为元数据持久化，影响 viewport"适应宽度"基准可后续接入）——任务卡"调整画布页面尺寸"标注为 MVP 范围外（画布全屏是 Phase 2 架构既定）。
- 模板预览为自绘简化（Canvas 多实例 context 限制），视觉与 PaperRenderer 近似。
