# Phase 154 修复总结：缩略图缓存身份成对发布

## 修复

`LibraryPage.refreshThumbnailGeneration()` 现在只在 revision 与 PixelMap 同时存在时复用缓存；渲染未产生 PixelMap 时不发布 revision。空缓存不会再在相同内容版本内永久阻止重画。

generation、3-worker 并发上限、64 项 LRU 和 PixelMap 释放逻辑保持不变。

## 验证

`d02-thumbnail-cache-pair.mjs`：`TOTAL=3 FAILED=0`。

本阶段未启动设备、模拟器、虚拟机或 Hypium；快速滚动与实际 PixelMap 峰值仍待 M2-RELEASE。
