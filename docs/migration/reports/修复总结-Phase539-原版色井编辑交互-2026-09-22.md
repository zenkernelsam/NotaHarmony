# Phase 539：原版 Favorite Color Well 编辑交互落地

日期：2026-09-22

## 选题

Phase 532 遗留项：色井持久化（`favorite_color_well` + 仓储写/删 + VM
方法）早已就绪，但 `ColorPickerView` 只有“点选取色”，缺原版的
长按编辑/删除/`+` 追加交互。Phase 538（便签卡文件夹色片）提交后
以本项接续。

## 原版现场重放（decompiled_1.0.3）

- 事件族 `rh9`：`xg9` OnAddColorClick(toolType,index)、`bh9`
  OnDeleteColorClick(pb4)、`ah9` OnColorWellValueChange
  (wellId,wellIndex,newColor)、`yg9` OnBrushColorClick；
  `ti9.java` 统一 dispatch 至 `g0`/`z47`/`xh9`/`ku5`。
- `wj9.java` case 17：色轮把编辑色回写井——`new ah9(wellId,
  kkf.c0(editorColor), wellIndex)`。
- `jm3.java`：`+` 入口 → `xg9`；`ys2.java`：逐工具菜单 → `bh9`。
- `tb4.java`：删井 `trayIndex>删除位 → -1` 致密化；`vb4.java`
  删行；`qb4.java` 实体 (id, toolType, color, trayIndex)。
- 原版字符串：`ui_tools__add_a_color`/`color_options`/
  `open_color_wheel`/`delete`。

## Harmony 落地

- `EditorViewModel.addFavoriteColorWell()`：以
  `favoriteColors.length` 为下一 trayIndex 写入当前
  `brushColor`（`xg9` 追加语义）；写/删沿用既有
  `setFavoriteColorWell`/`removeFavoriteColorWell`。
- `ColorPickerView`：井点 `bindContextMenu(LongPress)` →
  “设为当前颜色”（`ah9` 写井）/“删除”（`bh9`+`tb4` 致密化）；
  网格尾 `+` 虚线圆片 → `addFavoriteColorWell()`；
  全部新回调 `photoImportLeaseActive` fail-closed。
- 字符串：`add_a_color`、`well_set_current_color`（英/中）。

## 验证

- `d02-original-color-well-edit.mjs`：24 断言全过（原版事件/
  存储锚点 + Harmony 锚点 + 增写删致密化可执行模型）。
- 全套 replay：**434 PASS / 0 FAIL**。
- `note@default`、`note@ohosTest`：BUILD SUCCESSFUL（仅存量警告）。

## 登记差异

- 原版写井色经色轮回传（`wj9`→`ah9`）；Harmony 长按菜单直接以
  当前刷色写入——持久化路径一致，交互简化，已登记。
- 原版删除入口在逐工具菜单；Harmony 收敛为井点长按菜单，
  `bh9`/`tb4` 语义不变。
- 未发现原版井数上限，`+` 追加不封顶（登记观察项）。
