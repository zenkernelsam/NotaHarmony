# Phase 584 — 原版 Tape 工具入箱（REVIEW 默认工具态，2026-09-28）

## 原版证据

`decompiled_1.0.3/defpackage`：

- `a6f.java`：工具枚举 REVIEW = ordinal 10（字段 `a6f.S`）。
- `rz1.java:1075`：Secondary 托盘默认 [POINTER, LASER, ZOOM, REVIEW,
  RULER]，REVIEW 在 index 3，brush 态
  `w31(-1706497, 36.0f, null, 0, 1, 0)` —— 首色井 -1706497、中档宽度
  井（[12,36,64] 的 36）、tapePattern=0。
- `w31.toString()`：`BrushState(..., tapePattern)`；0 ↔ `ife.STRIPES`。
- `dm2`：tapePattern 仅 TAPE 工具 CreateInkOp 合法。

## Harmony 改动

- `EditorViewModel.ets`：`createDefaultStates` 增 `'tape'`（REVIEW,
  Secondary idx3, w31 全字段对齐）；`createState` 扩展
  tapePattern/井位参数；`activeTapePattern`（缺省 STRIPES）承接激活行；
  `getRenderSpec()` 仅 REVIEW 下发 tapePattern；`supportsBrushControls`
  纳入 REVIEW。
- `ToolboxSettingsDialog.ets`：`toolTypeLabel` REVIEW → `tape_tool`。
- 触摸分发零改动：REVIEW 落入既有笔迹管线，tapePattern 直通
  StrokeSession → 笔画数据 → 图案渲染（继承 Phase 582/583 行为）。

## 影响

胶带工具可选可画：工具栏出现 Tape 行，画出的笔画带
`renderSpec.tapePattern` —— 自动获得置顶渲染、Hide/Reveal 门控、
9 图案 tile 渲染与持久化往返（既有管线全部命中）。

## 验证

- 专项 `d02-original-tape-tool-entry.mjs`：16/16。
- `note@default` 构建 0 错误。

## 范围外

图案选择器（mh9/nh9）与点按揭示（xtc）为后续阶段；存量箱回填行落
Secondary 尾部而非 index 3（已记录偏差）。
