# 修复总结 — Phase 468 视口续体共享租约修复

## 问题

视口按钮已禁用，但导入前启动的手势与定时器仍可在照片导入窗口内继续：PDF 栅格定时器
会异步重栅格、替换位图并强制重绘；视图状态防抖定时器会执行数据库写入。手势结束或取消
也会立即安排零延迟刷新。

## 修复

PDF 栅格调度在生命周期门禁后按序拒绝共享导入和历史租约；普通视图保存同样拒绝两类租约，
销毁终存保留豁免。捏合和平移的结束/取消回调在导入期只复位手势状态，不安排零延迟刷新。
视口模型、渲染管线、PDF 预算和终存语义不变。

## 影响与验证

- 新增 ADR-0440、Harmony evidence 和中文 Report。
- 新增专项 Replay `d02-viewport-continuation-shared-ingress-bound.mjs`，当前
  `TOTAL=10 FAILED=0`。
- 视口保存生命周期 Replay 已同步并通过；PDF 可见区栅格 Replay 保持通过；ArkTS 目标检查
  无错误，仅既有信息级提示。
- 全量 Desktop Replay 通过 409/409（29.316 秒）；clean 3.228 秒、ohosTest HAP 51.226 秒、
  default HAP 4.457 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
