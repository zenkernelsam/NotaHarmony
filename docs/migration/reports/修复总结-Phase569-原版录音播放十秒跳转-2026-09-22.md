# Phase 569：原版录音播放 ±10 秒跳转

日期：2026-09-22

## 原版现场

- `d32.java` case 6/7：播放器迷你控制条渲染 `10secsback_outline` /
  `10secsforward_outline` 图标按钮，a11y 标签
  `cd_rewind_10_seconds` / `cd_forward_10_seconds`。
- `yna.java` → `npa.i` → `uw7.s()`/`r()`：
  - 快退 `s()`：`newPos = max(position - 10000, 0)` → seek；
  - 快进 `r()`：`newPos = min(position + 10000, duration)` → seek。
- position/duration 为 `vna.o`/`vna.p` 聚合 ms 值。

## Harmony 落地

- `RecordingPanel` 时间轴行在位置/时长读数之间居中加 `SkipButton` 对
  （`↺ 10s` / `10s ↻`），a11y 用原版文案。
- 点击：`target = clamp(cumulativePositionMs ± 10000, 0,
  cumulativeDurationMs)` → `onSeek(target)`，聚合时间轴寻址与原版
  `vna.o`/`p` 同语义。
- 使能复用 slider 的 `controlsEnabled && canSeek()`，守卫复用
  `photoImportLeaseActive || loading`。

## 差异登记

- 图标换文本按钮（`↺ 10s`/`10s ↻`）；a11y 与语义不变。

## 验证

- Replay：`d02-original-recording-skip-10s.mjs` 17/17；全量 464 文件全绿。
- 构建：`note@default` 与 `note@ohosTest` HAP 均 BUILD SUCCESSFUL。
