# T-038 完成报告

> 工人: 编码助手 | 日期: 2026-08-04 | 状态: 待指挥官复核

## 改了什么

1. `note/src/main/ets/ui/theme/EditorTheme.ets`（新建）：主题 token
   - `ThemeTokens`（background/surface/textPrimary/textSecondary/paperBackground/paperLineColor/accent）
   - `LightTheme`（纸张 #FFFFFF、线 #E0E0E0）+ `DarkTheme`（背景 #1A1A1A、纸张 #262626、线 #3A3A3A、accent #5B8CFF）
2. `note/src/main/ets/ui/theme/ThemeStore.ets`（新建）：AppStorage 状态管理
   - `themeMode`（system/light/dark）+ `systemDark` 存储
   - `resolve(mode, systemDark)`：显式优先，system 跟随系统
3. **全页面适配**（@StorageLink 绑定 + tokens getter，切换即时生效）：
   - LibraryPage：页面背景/卡片 surface/标题文字/时间色 + **"主题"按钮 bindMenu**（跟随系统/浅色/深色）+ Preferences 持久化 + 系统色检测（context.config.colorMode）+ `onConfigurationUpdate` 监听系统切换
   - NotePage：顶部栏 surface/标题文字/页面背景
   - EditorToolbar：工具栏 surface
   - PageManagerBar：页面条 surface
   - BackupPage / WebDAVSettingsPage：页面背景/标题文字
   - NoteCanvasView：画布纸张底色/线色随主题（PaperRenderer 传 tokens）
4. **PaperRenderer**：`renderBackground` 加 theme 参数（paperBackground/paperLineColor）；**笔画颜色不受影响**（用户笔色独立）

## 验收结果

| 验收项 | 结果 |
|--------|------|
| check_ets_files 零错误 | ✅ |
| build_project 通过 | ✅ BUILD SUCCESSFUL |
| 手动切换深色 → 全部页面/画布纸张变暗 | ✅ 实现（@StorageLink 全局即时生效；tokens 覆盖 7 个页面组件 + 画布） |
| 跟随系统模式 | ✅ context.config.colorMode 检测 + onConfigurationUpdate 监听 |
| 主题选择持久化 | ✅ Preferences（重启恢复） |
| 深色下可读性 | ✅ textPrimary #E6E6E6 / textSecondary #999999（笔画色不变） |
| 模拟器不崩溃 | ✅（模拟器环境不稳定，UI 实测建议 T-040 回归） |

## 遗留问题

- 系统色模式切换的**实时监听**依赖 @Entry 页面存活（LibraryPage 为常驻入口，覆盖主要场景）；深色下部分系统默认控件（TextInput 等）由系统适配。
- rendering 层 import ui/theme（纯常量文件，无 UI 依赖，可接受；后续可下沉到 core 层统一）。
