# 修复总结 — Phase 471 工具栏直改共享租约修复

## 问题

颜色与宽度面板已在 Phase 470 纳入共享导入租约，但工具栏直改路径仍可在已打开的导入窗口内
修改内存工具状态：普通工具切换、笔刷样式切换、选区手绘/矩形切换、紧凑更多入口及其工具菜单。

## 修复

上述入口统一消费父页共享照片导入租约：相关按钮禁用，直改回调增加第一层租约拒绝；选区样式
按钮在原有 TAPER 可用性规则上叠加禁用。页面级信号回调、工具模型、保存链、历史与选区语义不变。

## 影响与验证

- 新增 ADR-0443、Harmony evidence 和本中文 Report。
- 新增专项 Replay `d02-toolbar-direct-mutations-shared-ingress-lease-bound.mjs`，
  当前 `TOTAL=11 FAILED=0`。
- 相邻工具控制、历史工具栏、选区样式和文本层共享租约 Replay 全部通过。
- ArkTS 目标文件无错误。
- 全量 Desktop Replay 通过 412/412（33.574 秒）；clean 3.630 秒、ohosTest HAP 10.525 秒、
  default HAP 58.301 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
