# Phase 582 — 原版 Tape 元素置顶渲染序（2026-09-28）

## 原版证据

`decompiled_1.0.3/defpackage/vnd.java` `compareTo`：先比较
`ly3.k()`（isTape，`s06.k == u16.TAPE`，ShapeImpl 同）——仅一侧
为 tape 时 tape 排最后，即 **tape 恒渲染于全部非 tape 元素之上**，
与 zIndex 无关；tape 之间保持常规排序。`dm2` 校验 tapePattern 仅
TAPE 工具可携带。

## Harmony 改动

`core/model/PageElementOrder.ets` `materializePageElements`：物化后
稳定分区——tape 元素（笔画 `renderSpec.tapePattern` 非空；形状
`originalTool === 3`）移至末尾保相对序；transient 在制笔画仍最后。

## 影响

原版 study tape 语义要求胶带永远遮盖下层内容；此前按 zIndex 渲染，
其他元素置顶会破坏遮盖。现已对齐。

## 验证

- 专项 `d02-original-tape-zorder.mjs`：7/7。
- `note@default` 构建 0 错误。

## 范围外

REVIEW 笔画创建链路（getRenderSpec 发 tapePattern）、pattern picker、
hide/reveal 开关、点按揭示——后续阶段。
