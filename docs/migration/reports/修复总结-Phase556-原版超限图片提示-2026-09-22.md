# 修复总结 — Phase 556 原版超限图片提示（2026-09-22）

## 原版行为

`u49`/`rd9`：图片插入超限时 snackbar 提示 "Image is too large to add."，
与通用失败提示区分。

## 本次改动

- `OriginalPhotoIngress.ets`：新增 `OriginalPhotoTooLargeError`；两处超限
  闸口（stat 预检 + 读取后校验）改为类型化抛出。
- `NoteCanvasView.ets`：新增 `photoErrorToastRes` 映射；相册导入、剪贴板
  图片、拍照三个 catch 面统一按类型弹 `image_too_large_to_add` 或通用
  `original_photo_insert_failed`。
- 双语字符串：`image_too_large_to_add`（EN 原文 / zh "图片过大，无法添加"）。

## 差异登记

原版按解码像素判超限，Harmony 按 100MB 字节闸口判定；snackbar→toast 平台
适配。

## 验证

- 新回放 `d02-original-image-too-large.mjs`：12 项断言全过。
- 全量回放、双 HAP 构建通过。
