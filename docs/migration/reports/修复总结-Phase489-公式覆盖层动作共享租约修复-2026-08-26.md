# 修复总结 — Phase 489 公式覆盖层动作共享租约修复

## 问题

已打开公式编辑覆盖层已有响应式禁用，父页三个回调也有共享导入/历史防线；但输入、取消和完成
是组件内直接转发边界，导入收口窗口内的晚到事件仍可能先改写草稿或触发流程。

## 修复

三个回调先拒绝 `photoImportLeaseActive`，再执行原有转发。父页共享/历史守卫、LaTeX 校验、
预览生成、插入事务、撤销语义和失败反馈不变。

## 影响与验证

- 新增 ADR-0461、Harmony evidence 和本中文 Report。
- 扩展已打开公式共享租约专项 Replay 锁定三项新防线，当前 `TOTAL=13 FAILED=0`。
- ArkTS 目标无错误，仅既有警告与信息级提示。
- 相邻公式提交与公式插入共享租约 Replay 通过。
- 全量 Desktop Replay `REPLAY_FILES=421 PASSED=421 FAILED_FILES=0`
  （29.975 秒）；clean 3.211 秒、ohosTest HAP 9.951 秒、default HAP 29.523 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
