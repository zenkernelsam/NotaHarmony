# 修复总结 — Phase 467 粘贴共享租约修复

## 问题

照片导入窗口内，长按菜单仍可启动系统剪贴板图片导入；普通粘贴可预留 identity、执行
Group Paste 持久化并入栈历史。缩放与适配宽度也会触发防抖视图状态写入，与导入收口竞争。

## 修复

粘贴可用性先拒绝共享导入再拒绝历史租约；粘贴入口增加双层防御。缩放加减和适配宽度在
导入期禁用。系统剪贴板导入自身门禁不变，剪贴板内容、粘贴事务和防抖保存语义不变。

## 影响与验证

- 新增 ADR-0439、Harmony evidence 和中文 Report。
- 新增专项 Replay `d02-paste-shared-ingress-lease-bound.mjs`，当前
  `TOTAL=10 FAILED=0`。
- 相邻选区、视口范围和视口保存生命周期 Replay 通过；ArkTS 目标检查无错误，仅既有信息级
  提示。
- 全量 Desktop Replay 通过 408/408（29.822 秒）；clean 3.509 秒、ohosTest HAP 51.543 秒、
  default HAP 4.448 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
