# Phase 331 修复总结：录音捕获持久化销毁绑定

日期：2026-08-23（Asia/Shanghai）

## 缺陷与修复

- `persistCapturedRecording()` 在持久化 await 后无条件调用 `loadRecordings()`；编辑器在写入期间被销毁时，
  过期续体仍可对已销毁页面刷新录音列表和时间线。
- 现在持久化完成后检查 `editorDisposed`，销毁态跳过录音刷新。durable 捕获结果保持权威。

## 产物与验证

- ADR：`docs/migration/adr/ADR-0308-recording-persist-disposal-bound.md`
- Evidence：`docs/migration/evidence/recording-persist-disposal-bound-harmony-2026-08-23.md`
- Replay：`docs/migration/replays/d02-recording-persist-disposal-bound.mjs`（2/2 通过）。
- 全量 Desktop Replay：`REPLAY_FILES=298 PASSED=298 FAILED_FILES=0`。静态双 HAP 跳过签名并通过：
  `note@default` 12.803 秒，`note@ohosTest` 8.852 秒。
- 未启动模拟器、虚拟机、真机或 Hypium；真实保存中退出矩阵保留设备验收。
