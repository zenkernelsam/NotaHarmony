# Phase 166 修复总结：Note 导入输入大小预算

## 发现

系统 picker 导入路径原先按 `stat.size` 直接分配完整内存，再交给 ZIP 预算校验。超大输入可能在解析安全边界前造成内存压力。

## 修改

- `note/src/main/ets/data/ZipArchive.ets`
- `note/src/main/ets/data/NoteImporter.ets`
- 新增 `ADR-0143-note-import-input-size-budget.md`
- 新增 `d02-note-import-input-size-budget.mjs`

新增 `ZIP_MAX_ARCHIVE_BYTES` 统一入口预算；导入在 `readFileFully` 前拒绝超限或非安全整数文件，并保证句柄关闭。

## 验证

- Replay：`TOTAL=4 FAILED=0`
- `note@default assembleHap`：本轮未执行，仓库无 `hvigorw` 启动脚本；此前同一工作树记录为 `BUILD SUCCESSFUL`
- `note@ohosTest assembleHap`：本轮未执行，仓库无 `hvigorw` 启动脚本；此前同一工作树记录为 `BUILD SUCCESSFUL`
- 未启动设备、模拟器、虚拟机或 Hypium。

## 未闭环

真实超大文件、磁盘空间不足和用户提示仍需设备运行态验收。
