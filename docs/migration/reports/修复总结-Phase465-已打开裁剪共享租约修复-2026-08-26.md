# 修复总结 — Phase 465 已打开裁剪共享租约修复

## 问题

照片导入可在图片裁剪会话保持打开时开始；此时把手拖动、重置、关闭和确认仍可操作。
这些动作会改写裁剪草稿、重建选区、发布墨迹控件状态或提交 TRANSFORM_ELEMENTS 历史，
与照片结果合并竞争。

## 修复

已打开裁剪层接收共享照片导入租约并禁用全部副作用控件；四个回调再显式拒绝共享导入
和历史租约。启动裁剪、确认入口、画布触摸和销毁清理保持原有防御，裁剪事务与撤销语义不变。

## 影响与验证

- 新增 ADR-0437、Harmony evidence 和中文 Report。
- 新增专项 Replay `d02-open-crop-shared-ingress-lease-bound.mjs`，当前
  `TOTAL=10 FAILED=0`。
- 相邻裁剪共享租约与本地图片裁剪 Replay 通过；ArkTS 目标检查无错误，仅既有信息级提示。
- 全量 Desktop Replay 通过 406/406（31.777 秒）；clean 3.336 秒、ohosTest HAP 57.408 秒、
  default HAP 5.126 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
