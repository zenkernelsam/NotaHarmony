# Phase 545：原版新建笔记快捷动作

日期：2026-09-22

## 选题

原版库内新建卡片 FAB 展开快捷动作（Record audio / Import file /
Scan / Capture）；Harmony "+" FAB 仅能建空白笔记。

## 原版现场重放

- `ksh.g` + `t6j.b`：卡片内嵌 FAB，`mw3` case 0 渲染
  `p40.b(icon,label,callback)` 动作行——record_audio（sc 组合回调）、
  import_file、scan（`z2` 能力门）、capture_and_add（`function4`
  可空门）。
- `oi5`/`wi5`：卡片本体点击 = 默认建笔记。

## Harmony 落地

- FAB 改 speed-dial：`createMenuOpen` 展开/收起（+/× 切换），
  展开列 New note → Record audio → Import file（原版动作序，
  空白创建为首项等价卡片语义）。
- `createAndRecord`：`createAndLaunch(true)` → `autoRecord=1`
  路由参数 → `NotePage` 加载完成后 `showRecordings=true` +
  `startRecording()`。
- `importAndOpen`：`importFromFile` 系统选择器 → 刷新列表 →
  打开导入笔记；`ImportResult.CANCELLED` 区分取消（静默）。
- `BackupPage` 对 CANCELLED 静默（修复取消误报导入失败）。

## 验证

- `d02-original-new-note-quick-actions.mjs`：24 断言全过。
- `library-note-create-delete-lifecycle-bound` 锚点演进至
  `createAndLaunch`/`importAndOpen`（守卫不变）。
- 全套 replay：**440 PASS / 0 FAIL**；`note@default` +
  `note@ohosTest` BUILD SUCCESSFUL。

## 登记差异

- scan/capture 无可移植后端；动作归属由卡片内嵌 FAB → 全局
  创建 FAB（登记）。
