# 修复总结 — Phase 552 原版从模板新建笔记（2026-09-22）

## 原版行为

`cd.java` case 0 新建表单：Import → Templates（`ac4.K0` flag）→ Doc scan
（`ac4.a0` flag）→ Create note。Templates 行打开模板选择页，以所选纸张新建
笔记，不改变默认模板设置。

## 本次改动

- `LibraryPage.ets`：speed-dial 末尾新增 `templates` chip；新增
  `TemplatePickerDialog`（PLAIN/LINES/GRID/DOTS 四款纸张网格）；新增
  `createFromTemplate(template)` → `createAndLaunch(false, templateOverride)`，
  复用全部守卫与失败语义。
- `LibraryViewModel.createNote(title, templateOverride?)` 与
  `NoteRepository.createNote(..., templateOverride?)`：覆盖时经
  `applyOriginalPaperSettings` 只替换纸张类型，尺寸/方向继承解析出的默认
  模板，照常走 `persistOriginalBlankNoteBootstrap`，不改全局偏好。
- `RepositoryInterfaces.ets` 契约同步。
- 新增字符串：`templates` / `template_plain` / `template_lines` /
  `template_grid` / `template_dots`（双语）。

## 差异登记

原版完整模板管理页（`ac4.K0` flag）与 Doc scan（`ac4.a0` flag）不可移植；
Harmony 仅提供四款内置纸张的一键新建，与原版 Templates 行的"选择→新建"
语义一致。

## 验证

- 新回放 `d02-original-create-from-template.mjs`：14 项断言全过（原版行序/
  字符串锚点 + Harmony 芯片/对话框/管线锚点 + 覆盖语义模型）。
- 全量回放与 `note@default`、`note@ohosTest` 双构建通过。
