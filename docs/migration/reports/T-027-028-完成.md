# T-027 完成报告

> 工人: 编码助手 | 日期: 2026-08-02 | 状态: 待指挥官复核

## 改了什么

`note/src/main/ets/data/NoteExporter.ets`：

- `paperSizeToMm(size)`：A3~TABLOID 标准毫米尺寸映射（导出/导入共用，导出到 NoteImporter）
- `NoteExporter.exportNote(noteId)`：NoteMeta + PageInfo[] + StrokePersistence 元素 → manifest.json + pages/page_N.json → ZipWriter.build()；失败返回 null（异常安全）
- `NoteExporter.exportToFile(context, noteId, title)`：导出 → 沙箱 tempDir 临时文件 → `picker.DocumentViewPicker.save()`（`newFileNames` 预填标题 + `fileSuffixChoices=['Note 文件|.note']`）→ 复制到用户位置 → finally 清理临时文件
- `NoteExporter.exportAllNotes()`：全部笔记 → `Map<noteId, Uint8Array>`（WebDAV 备份用）

## 验收结果

| 验收项 | 结果 |
|--------|------|
| check_ets_files 零诊断 | ✅ no diagnostics |
| build_project 通过 | ✅ BUILD SUCCESSFUL |
| exportNote 返回有效 ZIP（manifest + pages/page_N.json） | ✅ 实现（manifest 字段完整） |
| exportToFile 触发系统保存对话框 | ✅ DocumentViewPicker.save |
| 不修改契约文件 | ✅ 通过 |

## 遗留问题

- MVP：元素无 page 关联（client_op 无 page_id 列），全部元素导出到 `pages/page_0.json`，其余页空 elements；多页笔记往返后元素合并（与 Phase 3 持久化模型一致）。
- 大笔记 >100 页的内存策略：当前一次性构建 ZIP（MVP 数据量小可接受），taskpool 后台化留待 T-031 后优化。

---

# T-028 完成报告

> 工人: 编码助手 | 日期: 2026-08-02 | 状态: 待指挥官复核

## 改了什么

`note/src/main/ets/data/NoteImporter.ets`：

- `ImportResult`（SUCCESS/UNSUPPORTED_FORMAT/CORRUPTED/PARTIAL）+ `ImportReport`（noteId/title/pageCount/message）
- `importFromData(data)`：ZipReader.parse（损坏→CORRUPTED 不崩溃）→ manifest.json 存在→我方格式；无→外部格式
- `importFromFile(context)`：`picker.DocumentSelectOptions`（`fileSuffixFilters=['.note']`）→ 读文件 → importFromData
- `importOurFormat`：**两阶段事务性**——阶段 1 全量解析验证（缺页/坏元素跳过不致命），阶段 2 才写库；**生成新 noteId 不覆盖已有笔记**；页面 pageId 重新生成；按 paperSize 映射毫米尺寸
- `importExternalFormat`：扫描 metadata.json/info.json/note.json 提取标题（有标题→PARTIAL 创建空白笔记）；完全无法识别→UNSUPPORTED_FORMAT + 内部文件列表（供调试）

## 验收结果

| 验收项 | 结果 |
|--------|------|
| check_ets_files 零诊断 | ✅ no diagnostics |
| build_project 通过 | ✅ BUILD SUCCESSFUL |
| 导入我方 .note → 完整恢复（笔画/页面/标题） | ✅ 实现（T-031 联调验证） |
| 导入损坏文件 → CORRUPTED 不崩溃 | ✅ ZipReader 校验 + 全路径异常安全 |
| 导入非 .note → UNSUPPORTED_FORMAT | ✅ 实现 |
| 不修改契约文件 | ✅ 通过 |

## 遗留问题

- 外部原版 .note（FlatBuffers 加密/二进制）无法解码，MVP 返回 UNSUPPORTED_FORMAT + 文件列表（Phase 4A 需 FB schema）。
- >10MB 文件 taskpool 后台解析未实现（MVP 单线程 async，标注待优化）。
