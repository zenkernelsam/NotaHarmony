# T-021 完成报告

> 工人: 编码助手 | 日期: 2026-08-02 | 状态: 待指挥官复核

## 改了什么

1. `note/src/main/ets/rendering/UndoRedoManager.ets`：op 栈管理器（UndoableActionType: ADD_STROKE/DELETE_STROKE/ERASE_STROKES/TRANSFORM_STROKES；undoStack/redoStack 各最多 50 步；push 清空 redoStack；undo/redo 返回操作供调用方执行）
2. NoteCanvasView 集成：
   - 笔画完成 → push ADD_STROKE；擦除 → push ERASE_STROKES（removedStrokes）；删除 → push DELETE_STROKE；变换 → push TRANSFORM_STROKES
   - Undo/Redo 信号（父组件 @Prop @Watch）→ undoStroke/redoStroke → applyAction 反向/正向执行 → rebuildFromStrokes 重绘
   - 工具栏 Undo/Redo 按钮 enabled 状态经 onUndoRedoChanged 回调同步（@Prop canUndo/canRedo）

## 验收结果

| 验收项 | 结果 |
|--------|------|
| check_ets_files + build_project 通过 | ✅ 通过 |
| 画一笔 → Undo → 笔画消失 | ✅ 模拟器验证（深色像素 14318→0） |
| Undo → Redo → 笔画恢复 | ✅ 模拟器验证（0→14318） |
| 擦除 → Undo → 恢复擦除 | ✅ 实现（ERASE_STROKES 反向恢复） |
| 新操作后 Redo 禁用 | ✅ 实现（push 清空 redoStack） |
| 不修改契约文件 | ✅ 通过 |

## 遗留问题

- 无。

## 集成修复记录

- 工具栏 canUndo/canRedo 初版为普通属性，父组件 @State 变化未同步到子组件（Undo 按钮始终 disabled 点击穿透）→ 改为 @Prop 后正常。
