# Phase 541：原版笔记上下文菜单补齐

日期：2026-09-22

## 选题

原版 `d5j` 笔记卡片菜单完整序：Rename → Favorite/Unfavorite →
Duplicate → Export → Export options → Open in new window →
Show in folder → Sort to folder → Copy note ID → Report → Delete。
Harmony 仅有 Favorite/Move/Delete 三项——五个可移植项缺失。

## 原版现场重放

`d5j.java:112-268` 逐项固定（R.string 出现序）；`gj9` 翻转收藏
标签；export_options/open_in_new_window/report 为条件项。

## Harmony 落地

- **Rename**：`NameDialog` 增 `showDecoration` 门；接口新增
  `renameNote` → `updateNoteTitle`（原版 SET_TITLE op 管线，
  带 history）；VM `renameNote` 物化回填 + applySort。
- **Duplicate**：`exportNote`→`importFromData` 无损包往返，
  导入器分配新 note id（op 史不复制，登记）。
- **Export**：`exportToFile` 单笔记落盘 + toast。
- **Show in folder**：`setFolder(folderId)` 导航（复位 dk9 分区），
  未归档置灰。
- **Copy note ID**：pasteboard 明文 + toast。
- 菜单顺序与原版可移植子集一致；Delete 末位不变。

## 验证

- `d02-original-note-context-menu.mjs`：43 断言全过（d5j 顺序
  锚点 + Harmony 锚点 + 可移植子集顺序模型）。
- 全套 replay：**436 PASS / 0 FAIL**；`note@default` +
  `note@ohosTest` BUILD SUCCESSFUL。

## 登记差异

- export_options（分享面板）/open_in_new_window（多窗口）/
  report_note（反馈服务）不可移植。
- Duplicate 以包往返实现（原版为 Realm 行级复制）；库投影等价。
