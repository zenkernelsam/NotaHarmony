# 修复总结 Phase 559 — 原版录音中指示徽标

日期：2026-09-22

## 原版证据

`decompiled_1.0.3` 的 `rej.a` 在录制会话活跃期间渲染编辑器顶部居中
徽标：

- `jub` 在场模型非空即显示，文案 `presence_recording` =
  "Recording in progress"（`f0f.java:626`，顶部居中 + 24dp 上 padding）。
- 协作场景改用 `presence_recording_by_user`（"%1$s is recording"）。
- 徽标随会话全程存在（含暂停）。

## Harmony 落地（`NotePage.ets`）

顶部导航行（标题之后、Recordings 按钮之前）加徽标：红点 + "正在录音"
文本，`accessibilityText` 同文案；显示条件
`isOriginalRecordingCaptureActive(recordingSessionSnapshot.captureState)`
覆盖 STARTING/RECORDING/PAUSING/PAUSED/RESUMING/STOPPING —— 与原版
会话级可见性一致。此前该状态只喂给 RecordingPanel，面板关闭时无
任何录制中提示。

## 字符串

`presence_recording`："Recording in progress" / "正在录音"（英文取原文）。

## 登记差异

- `presence_recording_by_user`（"%1$s is recording"）是协作特性，
  Harmony 无共享在场模型，不移植。
- 挂载点为导航行而非悬浮顶部居中（该层无 overlay 脚手架）。
- 徽标纯展示，与原版一致（不可交互）。

## 验证

- 新增 `d02-original-recording-presence-indicator.mjs`（8 断言）。
- 全量 Desktop Replay 454/454 全绿。
- `note@default` 与 `note@ohosTest` HAP 构建 0 错误。
- 未启动模拟器/真机/Hypium。
