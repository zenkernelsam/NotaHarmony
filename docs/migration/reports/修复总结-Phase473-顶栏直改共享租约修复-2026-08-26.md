# 修复总结 — Phase 473 顶栏直改共享租约修复

## 问题

工具栏收口后，顶栏仍有三条直改路径可在共享照片导入期生效：标题文本可进入编辑态，
编辑态输入可直接改写 `titleDraft`，录音面板外层开关可打开或关闭面板。

## 修复

标题点击、标题草稿输入和录音开关统一先拒绝共享导入租约；录音按钮同步禁用，开启与
关闭双向一致。标题提交与失焦继续走既有串行队列，未变更标题保持幂等发布，变更标题
由原有提交防御拒绝。录音控制器、持久化、历史和页面加载防御不变。

## 影响与验证

- 新增 ADR-0445、Harmony evidence 和本中文 Report。
- 新增专项 Replay `d02-topbar-direct-ui-mutations-shared-ingress-lease-bound.mjs`，
  当前 `TOTAL=5 FAILED=0`。
- 相邻标题共享租约 Replay 通过 10/10，录音面板共享租约通过 12/12。
- ArkTS 目标文件无错误，仅既有警告与信息级提示。
- 全量 Desktop Replay 通过 414/414（30.415 秒）；clean 3.456 秒、ohosTest HAP 9.835 秒、
  default HAP 29.402 秒均静态构建成功。
- 未启动模拟器、虚拟机、真机或 Hypium；T-042 继续保留为 Goal 最后任务。
