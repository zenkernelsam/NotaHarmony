# 原版证据：选区样式工具栏上下文绑定

- 日期：2026-08-25
- 枚举：1.0.3 `defpackage/y31.java` 定义 `Mono=0`、`Taper=1`、`Dash=2`、`Dot=3`，与当前 `BrushStyle` 对应。
- 工具栏状态来源：`defpackage/dhb.java` 在选中 Ink 控件分支读取 `t16VarW.ordinal()`，再反向映射为
  `y31.J/I/K/L`。该值进入样式按钮集合的回调与控件状态，不是固定默认样式。
- 结论：选中已有笔迹后，工具栏必须回显第一个合格笔迹的当前样式；铅笔等不可变宽场景仍需禁用 Taper。
