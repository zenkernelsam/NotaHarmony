# ADR-0144: 备份批次条目与总字节预算

## 决策

备份 manifest 限制最多 `10000` 个条目、总对象字节最多 `1 GiB`。解析远端 manifest 和发布本地 packages 都执行相同预算检查。

## 原因

只校验单条大小不足以保护恢复器；恶意或损坏 manifest 可通过大量合法小对象耗尽下载、内存和磁盘预算。发布侧提前拒绝也避免生成不可恢复的部分批次。

## 验收

静态 replay 检查共享常量、解析累计预算和发布前预算；真实 WebDAV 服务仍需后续验收。
