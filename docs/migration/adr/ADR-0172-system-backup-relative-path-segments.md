# ADR-0172：系统 Backup 相对路径按段校验

## 决策

Backup manifest 路径统一分隔符后按 segment 校验，只拒绝空段、`.`、`..`、绝对路径
和带盘符/冒号的路径；普通文件名中的连续点合法。

## 原因

旧实现只要字符串包含 `..` 就拒绝，会误伤 `draft..note` 等合法文件，导致整个系统备份
不可用。目录穿越的安全边界应是路径段，而不是任意子串。

## 边界

文件系统符号链接仍需 CoreFileKit 设备验证；本阶段修正纯路径语义。
