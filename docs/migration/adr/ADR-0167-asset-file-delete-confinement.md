# ADR-0167：资产文件删除路径隔离

## 决策

资产删除只允许 unlink 位于当前应用 `filesDir` 子路径内的文件；数据库中越界或含父目录
穿越的 `local_path` 只记录错误并拒绝删除，不影响已提交的数据库事务。

## 原因

`local_path` 来自持久化数据，不能作为可信文件系统路径。直接 unlink 会把损坏数据库或
恶意包转换为应用外任意文件删除。

## 边界

文件系统符号链接解析和设备权限故障仍需 CoreFileKit 真机 fault injection；当前门禁
至少阻止普通绝对路径越界和 `..` 穿越。
