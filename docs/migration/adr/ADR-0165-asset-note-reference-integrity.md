# ADR-0165：共享资产笔记引用完整性

## 决策

保存共享资产时，在同一事务内验证合并后的每个 `noteIds` 都存在于 `note_meta`；
任一引用为空或不存在则拒绝并回滚。

## 原因

资产引用以 JSON 集合保存，数据库无法用外键逐项约束。旧实现接受任意调用方 ID，
会制造孤儿引用，后续缩略图、删除和资产回收逻辑无法正确闭合。

## 边界

历史数据库中已经存在的孤儿 JSON 需要独立迁移扫描；本阶段阻止新孤儿继续写入。
