# ADR-0169：所有资产注册路径统一引用门禁

## 决策

图片 `CREATE_BLOCK` 和原版录音资产入库在写入 `note_asset` 前，复用共享的
`validateNoteReferences()`，验证合并后的全部 note ID。

## 原因

Phase188/189 已保护普通资产和图片字节接收，但原版兼容操作仍有直接 SQL 写入路径；
不统一会留下可由特定操作制造的孤儿引用。

## 边界

历史数据清理与真实同步服务端仍需独立迁移/设备验收。
