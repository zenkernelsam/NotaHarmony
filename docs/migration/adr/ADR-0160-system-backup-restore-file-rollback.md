# ADR-0160：系统 Backup 恢复文件级回滚

## 决策

恢复在全量预检后，将每个即将覆盖的现有文件先移动到 backup 目录的回滚暂存区，
再复制新文件；任一步失败时按逆序删除新文件并恢复旧文件，成功后删除回滚暂存区。

## 原因

仅预检源文件不能覆盖复制阶段的 I/O 失败。没有回滚时，恢复可能只写入批次前缀，
使应用处于混合版本状态。

## 边界

进程在单个 rename/copy 操作中途崩溃、文件系统不支持跨目录 rename，以及权限故障
仍需真实 CoreFileKit 设备 fault injection；这些情况不能由静态 replay 证明。
