# ADR-0164：文件夹兄弟序号归一化

## 决策

创建、移动和删除文件夹后的写事务保持兄弟文件夹的 `sibling_order` 为从 0 开始的
连续序列。移动同时重排旧父级和新父级；删除重排受影响父级。

## 原因

旧实现只追加 `max + 1`，不回收删除/移动留下的空洞。长期操作会造成无界序号，偏离原版
folder child-index 语义并使稳定排序依赖历史操作次数。

## 边界

真实原版 UI 拖拽动画和跨设备同步排序仍需设备验收；本阶段只修本地持久化顺序。
