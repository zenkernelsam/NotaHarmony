# ADR-0168：共享资产删除引用门禁

## 决策

`deleteAsset()` 只有在 `note_asset.note_ids` 为空时才允许删除数据库记录和本地文件；
仍被任何笔记引用的资产必须先通过解绑/笔记删除流程移除引用。

## 原因

原版 `NoteAsset` 保存共享 `noteIds`。无条件删除资产会让仍在使用它的图片或 PDF
变成缺失资源，并且文件删除不可由事务回滚。

## 验收

有引用资产拒绝删除且数据库/文件不变；无引用资产正常删除并清理文件。
