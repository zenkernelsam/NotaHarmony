# ADR-0128：系统备份 manifest 对象身份唯一

日期：2026-08-12

## 决策

恢复时以 `root + relativePath` 作为快照对象身份，manifest 中重复身份直接拒绝。重复项不能被当作无害冗余，因为它会重复写入目标文件，并可能让损坏的快照在预算内伪装成完整集合。

## 验收边界

该修复属于 M2-B-04 的安全补强；不改变原有 `filesDir/databaseDir` 范围、预算或 staging 发布协议。系统服务触发和设备级恢复仍由 M2-RELEASE 验收。
