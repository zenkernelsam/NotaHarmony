# ADR-0140: Binary plist 循环引用拒绝

## 决策

`BinaryPlistParser` 检测到对象引用环时设置解析错误并返回失败结果，不再以 `null` 占位并继续成功返回。

## 原因

循环引用会使容器内容无法完整还原。静默占位会把损坏或恶意 plist 伪装成可用导入，违反原版导入的内容完整性要求。

## 验收

Replay 检查循环分支调用 `fail`、不再产生 null 占位；HAP 构建和真实样本导入仍需后续环境验证。
