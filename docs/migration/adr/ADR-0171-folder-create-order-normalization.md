# ADR-0171：创建文件夹时归一化兄弟序号

## 决策

`createFolder()` 插入新文件夹后，在同一事务内归一化目标父级的兄弟序号。

## 原因

历史版本或外部修复可能留下 `sibling_order` 空洞。仅在 move/delete 时归一化会让一次
普通创建继续沿用不连续序号；创建也应遵守原版 child-index 顺序契约。

## 边界

只影响本地 folder 顺序，不宣称完成远端同步排序或拖拽动画验收。
