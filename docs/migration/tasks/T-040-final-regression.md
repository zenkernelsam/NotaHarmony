# T-040 最终集成回归

## 目标

将 T-034~T-039 全部集成，跑完整回归流程，修复集成冲突，确保 8-9 成体验的完整可用。

## 集成回归清单

在模拟器上逐步执行：

```
基础流：
1. 启动 → LibraryPage（有缩略图/搜索/排序）
2. 新建笔记 → 编辑标题 → Pen/Pencil/Highlighter 书写
3. 双指缩放画布 → 缩放后书写位置准确
4. 双指平移 → 浏览画布
5. 切换纸张模板（横线/方格/点阵）→ 背景变化
6. 切换纸张尺寸/方向 → 页面变化
7. 多页：添加/翻页/删除
8. 选区：框选 → 原生菜单 → 移动/删除 → Undo 恢复
9. 双击 → 文本框 → 输入 → 完成 → Undo
10. 画圆 → 形状识别 → Undo 恢复手画
11. Eraser 擦除 → Undo
12. 返回 LibraryPage → 缩略图显示笔画内容 → 标题正确
13. 创建文件夹 → 移动笔记 → 按文件夹浏览
14. 搜索过滤 → 排序切换
15. 切换暗色模式 → 全部页面适配 → 画布纸张变深
16. 导出 .note → 删除 → 导入 → 恢复
17. 重启应用 → zoom/主题/排序偏好保留
```

## 修复工作

- 各任务卡之间的代码冲突（NoteCanvasView 被多卡修改）
- viewport 变换与缩略图渲染的兼容
- 暗色模式与画布渲染的兼容
- 性能：大笔记缩放流畅度（如卡顿，优先优化脏矩形）

## 验收标准

- [ ] 17 步回归全部通过
- [ ] `check_ets_files` 全部文件零错误
- [ ] `build_project` BUILD SUCCESSFUL
- [ ] `start_app` 运行成功
- [ ] 连续操作 2 分钟无崩溃、无 hilog 应用级 ERROR
- [ ] 不修改 Phase 1 契约文件

## 完成报告

`docs/migration/reports/T-040-完成.md`（17 步逐项结果 + 修复的集成问题）
